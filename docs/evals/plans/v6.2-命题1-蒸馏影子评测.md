# 实验计划书（待命）：蒸馏单域 ≥ 旗舰 ×0.85——影子评测（v6.2 命题 1）

> **注册信息**
>
> | 项 | 值 |
> | --- | --- |
> | 版本 / 命题编号 | v6.2 命题 1（docs/V7路线图.md §四 研究命题表） |
> | 状态 | **待命·未执行**（豁免登记 E-3，docs/降级豁免登记-V7弧线.md）——外部依赖 A2 微调端点就位后按本计划书零决策开跑 |
> | 注册日期 | 2026-09-02 |
> | 新规约束 | R2 功效先行 / R3 统计措辞禁令 / R4 题面注册制 |

## ① 假设

- H1：经四段管道（采集→筛选→导出→训练）在单域轨迹上蒸馏的 8B 级模型，在该域任务上的成功率保持旗舰模型的 ≥85%（能力保持假设，非超越假设）。
- H0：Ratio < 0.85 或其置信下界 < 0.80。
- 判据（工程地板已锁定为代码双门，ShadowEvaluator）：**Ratio ≥0.85 且 RatioLower（95% Wilson 下界）≥0.80**。

## ② 效应量与功效计算

- 效应量假设：蒸馏与旗舰成功率差 ≤15pp 为检测目标（保持率口径换算）。
- 样本量：**禁止手估（R2 条款）**——执行时以 internal/eval/stats.go 的
  SampleSizeTwoProportion(p1, p2, 0.05, 0.80) 精确枚举输出为准（本计划书注册该工具为唯一权威）；
  配对同题设计优先（模板条款：配对显著降本）。
- 影子评测题量下限：≥100 题/域（与基线摸底同量级），最终以功效输出为准。

## ③ 分析计划

- 主指标：同题配对成功率比值 Ratio = 蒸馏臂 / 旗舰臂（二值化口径同 v6.1：沙箱终态确定性断言，判定器 internal/eval/asserts.go EvaluateAll——**禁止 LLM judge 做主指标**）。
- 检验：双门判定（Ratio 与 RatioLower），配对差值 McNemar 精确检验作描述性佐证。
- 次指标（仅描述性）：token 成本比、时延比、失败模式分布披露。
- 多重比较：单主指标，无校正。

## ④ token 成本估算

- 影子评测：n×2 臂 × 每题预算（≤60 轮/≤90 工具）≈ 与 v6.1 双臂同量级（数百万 token/域）。
- 训练成本在 A2 端点本地发生（框架零训练依赖不破，路线图 §四）。
- 蒸馏上线后回归评测切低价臂（弧线经济学自洽条款，模板 §成本）。

## ⑤ 执行前置

1. A2 就位（ollama 8B 或兼容微调端点，FineTuneBackend 连接器已实现）；
2. 冻结题面域标注核对（R4：题面 sha256 复验）；
3. 跑批后数字回填本文件 §⑥结果栏 + 豁免台账 E-3 标注「已复活」。

## ⑥ 结果

（待执行回填）
