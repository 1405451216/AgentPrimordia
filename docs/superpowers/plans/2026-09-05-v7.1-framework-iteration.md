# v7.1 框架迭代实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 五命题并行验收——V7 验证收尾 + 多模态 Agent + 规划增强 + 可观测性升级 + 工具智能统一，每个方向交付完整验收数字。

**Architecture:** 五条工作线全并行。第 1 周交付接口定义（interface + type），第 2 周起独立实现。V7 收尾全程后台运行不阻塞。每条新能力线走 TDD：先写测试 → 最小实现 → 单测通过 → bench A/B 实验 → McNemar 验收。

**Tech Stack:** Go 1.26+ 标准库；已有 `internal/eval`（McNemar/Wilson/asserts）、`internal/agent/multimodal`（ContentPart）、`internal/llm`（MultimodalProvider）、`internal/agent/planning`（Planner/DAG executor）、`internal/observability`（CorrelationStore）、`internal/otel`（OTLP/MetricExporter）、`internal/agent/tool_learning`（MemoryToolLearner）、`internal/tools/lifecycle`（AutoLoop）。

---

## 文件结构总览

```
新建:
  bench/multimodal/main.go              — 多模态 A/B 实验运行器
  bench/multimodal/tasks.json           — 20 道多模态题面
  bench/planning-v71/main.go            — 规划增强 A/B 实验运行器
  bench/intelligence/main.go            — 工具智能 A/B 实验运行器
  bench/intelligence/tasks.json         — 30 道工具密集型题面
  internal/agent/planning/decomposer.go — 子目标分解器
  internal/agent/planning/replanner.go  — 动态重规划
  internal/agent/planning/recovery.go   — 规划失败恢复
  internal/agent/planning/approval.go   — 人机协作审批
  internal/agent/planning/plan_state.go — 计划状态机
  internal/agent/planning/hooks.go      — 规划 Hook 集成
  internal/observability/alert.go       — 告警规则引擎
  internal/observability/dashboard.go   — 仪表盘 API（扩展现有 http.go）
  internal/observability/export/otel/   — otel/ 迁移目标位置
  internal/tools/intelligence/intelligence.go — 统一入口
  internal/tools/intelligence/create/detector.go — 缺口检测
  internal/tools/intelligence/create/creator.go  — 工具生成
  internal/tools/intelligence/optimize/profiler.go — 性能画像
  internal/tools/intelligence/optimize/tuner.go   — 参数调优
  internal/tools/intelligence/optimize/selector.go — 工具选择
  internal/tools/intelligence/reuse/catalog.go     — 工具目录
  internal/tools/intelligence/reuse/matcher.go     — 任务匹配
  internal/tools/intelligence/audit.go  — 全链路审计
  internal/tools/intelligence/hooks.go  — ReAct Hook

修改:
  internal/agent/react_convert.go       — 多模态消息转换（使用结构化 DTO）
  internal/agent/planning/planner.go    — 扩展 Planner 接口
  internal/agent/planning/planner_test.go — 补充测试
  internal/observability/observability.go — 扩展 CorrelationStore
  internal/observability/http.go        — 扩展仪表盘 API

迁移:
  internal/otel/*.go → internal/observability/export/otel/*.go
```

---

## Phase 0: 接口定义周（所有新能力线先交 interface + type）

### Task 1: 多模态接口扩展

**Files:**
- Modify: `internal/agent/multimodal/multimodal.go`
- Create: `internal/agent/multimodal/types_test.go`

现有 `ContentPart` 已覆盖 text/image_url/image_b64/audio/video。需新增 `MultimodalToolDef` 类型和 Hook 接口。

- [ ] **Step 1: 写测试**

```go
// internal/agent/multimodal/types_test.go
package multimodal

import "testing"

func TestContentPartImageURL(t *testing.T) {
    cp := ContentPart{Type: "image_url", URL: "https://example.com/img.png", MIME: "image/png"}
    if cp.Type != "image_url" {
        t.Errorf("expected image_url, got %s", cp.Type)
    }
    if cp.URL == "" {
        t.Error("URL should not be empty")
    }
}

func TestContentPartAudioBase64(t *testing.T) {
    cp := ContentPart{Type: "audio", Data: "base64data", MIME: "audio/wav"}
    if cp.Type != "audio" {
        t.Errorf("expected audio, got %s", cp.Type)
    }
}

func TestHasMultimodalWithImage(t *testing.T) {
    parts := []ContentPart{
        {Type: "text", Text: "描述这张图"},
        {Type: "image_url", URL: "https://example.com/img.png"},
    }
    has := false
    for _, p := range parts {
        if p.Type != "text" {
            has = true
            break
        }
    }
    if !has {
        t.Error("should detect multimodal content")
    }
}
```

- [ ] **Step 2: 运行测试确认通过**（现有 ContentPart 已支持这些类型）

```bash
cd agentprimordia && go test ./internal/agent/multimodal/ -run TestContentPart -v
```

- [ ] **Step 3: 提交**

```bash
git add internal/agent/multimodal/types_test.go
git commit -m "test: 多模态 ContentPart 类型验收测试"
```

### Task 2: 规划增强接口定义

**Files:**
- Modify: `internal/agent/planning/planner.go`
- Create: `internal/agent/planning/interfaces_test.go`

扩展 Planner 接口，新增 Replanner、RecoveryStrategy、ApprovalGate 接口。

- [ ] **Step 1: 在 planner.go 末尾新增接口定义**

```go
// 子目标分解器——将大目标拆解为有序子目标 DAG
type Decomposer interface {
    Decompose(ctx context.Context, goal string, context []string) (*Plan, error)
}

// 动态重规划器——执行偏离预期时重新分解
type Replanner interface {
    ShouldReplan(ctx context.Context, plan *Plan, observation string) (bool, string)
    Replan(ctx context.Context, plan *Plan, reason string) (*Plan, error)
}

// 失败恢复策略——死路检测 + 回退 + 替代路径
type RecoveryStrategy interface {
    DetectDeadlock(ctx context.Context, plan *Plan, failedSubTask string) bool
    Recover(ctx context.Context, plan *Plan, failedSubTask string) (*Plan, error)
}

// 审批门——高风险动作暂停等待人类确认
type ApprovalGate interface {
    RequiresApproval(ctx context.Context, action string) bool
    RequestApproval(ctx context.Context, action, reason string) error
    WaitApproval(ctx context.Context, action string) error
}

// 计划状态
type PlanState string

const (
    PlanStatePending   PlanState = "pending"
    PlanStateActive    PlanState = "active"
    PlanStateBlocked   PlanState = "blocked"
    PlanStateCompleted PlanState = "completed"
    PlanStateFailed    PlanState = "failed"
)

// ManagedPlan 带状态机的计划
type ManagedPlan struct {
    Plan      *Plan
    State     PlanState
    History   []PlanTransition
    CreatedAt time.Time
    UpdatedAt time.Time
}

// PlanTransition 状态转换记录
type PlanTransition struct {
    From PlanState `json:"from"`
    To   PlanState `json:"to"`
    At   time.Time `json:"at"`
    Why  string    `json:"why"`
}
```

- [ ] **Step 2: 写接口验收测试**

```go
// internal/agent/planning/interfaces_test.go
package planning

import "testing"

func TestPlanStateConstants(t *testing.T) {
    states := []PlanState{PlanStatePending, PlanStateActive, PlanStateBlocked, PlanStateCompleted, PlanStateFailed}
    seen := make(map[PlanState]bool)
    for _, s := range states {
        if s == "" {
            t.Error("plan state should not be empty")
        }
        if seen[s] {
            t.Errorf("duplicate state: %s", s)
        }
        seen[s] = true
    }
    if len(states) != 5 {
        t.Errorf("expected 5 states, got %d", len(states))
    }
}

func TestManagedPlanCreation(t *testing.T) {
    mp := &ManagedPlan{
        Plan:  &Plan{Goal: "test"},
        State: PlanStatePending,
    }
    if mp.State != PlanStatePending {
        t.Errorf("expected pending, got %s", mp.State)
    }
    if mp.Plan.Goal != "test" {
        t.Errorf("expected test goal, got %s", mp.Plan.Goal)
    }
}
```

- [ ] **Step 3: 运行测试**

```bash
cd agentprimordia && go test ./internal/agent/planning/ -run TestPlanState -v
cd agentprimordia && go test ./internal/agent/planning/ -run TestManagedPlan -v
```

- [ ] **Step 4: 提交**

```bash
git add internal/agent/planning/planner.go internal/agent/planning/interfaces_test.go
git commit -m "feat: 规划增强接口定义——Decomposer/Replanner/RecoveryStrategy/ApprovalGate"
```

### Task 3: 可观测性升级接口定义

**Files:**
- Create: `internal/observability/alert_test.go`
- Create: `internal/observability/export/otel/` (迁移准备)

新增 AlertRule 接口和 AlertEngine 类型定义。

- [ ] **Step 1: 写告警接口测试**

```go
// internal/observability/alert_test.go
package observability

import "testing"

func TestAlertRuleInterface(t *testing.T) {
    // 验证 AlertRule 接口定义正确
    var _ AlertRule = &thresholdRule{}
}

func TestAlertSeverity(t *testing.T) {
    severities := []AlertSeverity{SeverityInfo, SeverityWarning, SeverityCritical}
    for _, s := range severities {
        if s == "" {
            t.Error("severity should not be empty")
        }
    }
}

func TestAlertEvent(t *testing.T) {
    ae := AlertEvent{
        Rule:     "high_error_rate",
        Severity: SeverityCritical,
        Message:  "error rate > 50%",
    }
    if ae.Rule == "" || ae.Severity == "" || ae.Message == "" {
        t.Error("alert event fields should be populated")
    }
}
```

- [ ] **Step 2: 在 observability.go 末尾新增告警类型**

```go
// 告警严重度
type AlertSeverity string

const (
    SeverityInfo     AlertSeverity = "info"
    SeverityWarning  AlertSeverity = "warning"
    SeverityCritical AlertSeverity = "critical"
)

// AlertRule 告警规则接口
type AlertRule interface {
    Name() string
    Evaluate(store *CorrelationStore) ([]AlertEvent, error)
}

// AlertEvent 告警事件
type AlertEvent struct {
    Rule      string        `json:"rule"`
    Severity  AlertSeverity `json:"severity"`
    Message   string        `json:"message"`
    TraceIDs  []string      `json:"trace_ids,omitempty"`
    Threshold float64       `json:"threshold"`
    Actual    float64       `json:"actual"`
}

// thresholdRule 阈值告警规则（内部实现用于测试）
type thresholdRule struct {
    name      string
    threshold float64
    severity  AlertSeverity
    metricFn  func(store *CorrelationStore) float64
}

func (r *thresholdRule) Name() string { return r.name }
func (r *thresholdRule) Evaluate(store *CorrelationStore) ([]AlertEvent, error) {
    val := r.metricFn(store)
    if val > r.threshold {
        return []AlertEvent{{
            Rule:      r.name,
            Severity:  r.severity,
            Message:   r.name + " exceeded threshold",
            Threshold: r.threshold,
            Actual:    val,
        }}, nil
    }
    return nil, nil
}
```

- [ ] **Step 3: 运行测试**

```bash
cd agentprimordia && go test ./internal/observability/ -run TestAlert -v
```

- [ ] **Step 4: 提交**

```bash
git add internal/observability/observability.go internal/observability/alert_test.go
git commit -m "feat: 可观测性告警接口定义——AlertRule/AlertEvent/AlertSeverity"
```

### Task 4: 工具智能接口定义

**Files:**
- Create: `internal/tools/intelligence/intelligence.go`
- Create: `internal/tools/intelligence/intelligence_test.go`

统一入口接口 + 子模块接口。

- [ ] **Step 1: 写统一入口接口和类型**

```go
// internal/tools/intelligence/intelligence.go
package intelligence

import (
    "context"
    "sync"
    "time"
)

// ToolIntelligence 统一工具智能入口
type ToolIntelligence struct {
    mu       sync.Mutex
    detector GapDetector
    creator  ToolCreator
    profiler ToolProfiler
    tuner    ToolTuner
    selector ToolSelector
    catalog  *ToolCatalog
    matcher  TaskMatcher
}

// GapDetector 缺口检测器——从失败轨迹识别 missing_tool 模式
type GapDetector interface {
    Detect(ctx context.Context, trace []ToolCallRecord) ([]GapCandidate, error)
}

// ToolCreator 工具生成器——从缺口生成新工具
type ToolCreator interface {
    Create(ctx context.Context, gap GapCandidate) (*ToolArtifact, error)
}

// ToolProfiler 工具性能画像——per-tool 统计
type ToolProfiler interface {
    Record(ctx context.Context, usage ToolUsageRecord) error
    Profile(ctx context.Context, toolName string) (*ToolProfile, error)
    AllProfiles(ctx context.Context) (map[string]*ToolProfile, error)
}

// ToolTuner 参数调优器——基于性能画像数据驱动调优
type ToolTuner interface {
    SuggestTuning(ctx context.Context, toolName string, profile *ToolProfile) (*TuningSuggestion, error)
    ApplyTuning(ctx context.Context, toolName string, suggestion *TuningSuggestion) error
}

// ToolSelector 工具选择优化——学习最优选择策略
type ToolSelector interface {
    Select(ctx context.Context, task string, candidates []string) (string, error)
    RecordOutcome(ctx context.Context, toolName string, success bool) error
}

// 数据类型

type GapCandidate struct {
    Kind        string    `json:"kind"` // missing_tool / repeated_failure
    Key         string    `json:"key"`
    Count       int       `json:"count"`
    SampleError string    `json:"sample_error,omitempty"`
    FirstSeen   time.Time `json:"first_seen"`
    LastSeen    time.Time `json:"last_seen"`
}

type ToolArtifact struct {
    ID          string `json:"id"`
    Name        string `json:"name"`
    Description string `json:"description"`
    ArtifactSHA string `json:"artifact_sha"`
    Artifact    []byte `json:"artifact"`
}

type ToolCallRecord struct {
    ToolName  string        `json:"tool_name"`
    Args      string        `json:"args"`
    Result    string        `json:"result"`
    Error     string        `json:"error,omitempty"`
    Duration  time.Duration `json:"duration"`
    Success   bool          `json:"success"`
    Timestamp time.Time     `json:"timestamp"`
}

type ToolUsageRecord struct {
    ToolName string
    Success  bool
    Duration time.Duration
    Tokens   int
}

type ToolProfile struct {
    ToolName     string        `json:"tool_name"`
    TotalCalls   int           `json:"total_calls"`
    SuccessRate  float64       `json:"success_rate"`
    AvgDuration  time.Duration `json:"avg_duration"`
    P95Duration  time.Duration `json:"p95_duration"`
    AvgTokens    int           `json:"avg_tokens"`
    LastUsed     time.Time     `json:"last_used"`
}

type TuningSuggestion struct {
    ToolName    string            `json:"tool_name"`
    Parameter   string            `json:"parameter"`
    CurrentVal  string            `json:"current_val"`
    SuggestedVal string           `json:"suggested_val"`
    Confidence  float64           `json:"confidence"`
    Reason      string            `json:"reason"`
}

// NewToolIntelligence 构造器
func NewToolIntelligence(detector GapDetector, creator ToolCreator, profiler ToolProfiler, tuner ToolTuner, selector ToolSelector) *ToolIntelligence {
    return &ToolIntelligence{
        detector: detector,
        creator:  creator,
        profiler: profiler,
        tuner:    tuner,
        selector: selector,
        catalog:  NewToolCatalog(),
        matcher:  NewTaskMatcher(),
    }
}
```

- [ ] **Step 2: 写接口验收测试**

```go
// internal/tools/intelligence/intelligence_test.go
package intelligence

import "testing"

func TestGapCandidateCreation(t *testing.T) {
    gc := GapCandidate{Kind: "missing_tool", Key: "csv_parser", Count: 3}
    if gc.Kind != "missing_tool" {
        t.Errorf("expected missing_tool, got %s", gc.Kind)
    }
    if gc.Count != 3 {
        t.Errorf("expected 3, got %d", gc.Count)
    }
}

func TestToolProfileCreation(t *testing.T) {
    tp := ToolProfile{ToolName: "shell", TotalCalls: 10, SuccessRate: 0.8}
    if tp.ToolName != "shell" {
        t.Errorf("expected shell, got %s", tp.ToolName)
    }
    if tp.SuccessRate != 0.8 {
        t.Errorf("expected 0.8, got %f", tp.SuccessRate)
    }
}

func TestTuningSuggestionCreation(t *testing.T) {
    ts := TuningSuggestion{
        ToolName:     "shell",
        Parameter:    "timeout",
        CurrentVal:   "30s",
        SuggestedVal: "60s",
        Confidence:   0.75,
    }
    if ts.Confidence < 0 || ts.Confidence > 1 {
        t.Error("confidence should be in [0,1]")
    }
}
```

- [ ] **Step 3: 运行测试**

```bash
cd agentprimordia && go test ./internal/tools/intelligence/ -v
```

- [ ] **Step 4: 提交**

```bash
git add internal/tools/intelligence/
git commit -m "feat: 工具智能统一接口定义——ToolIntelligence/GapDetector/ToolProfiler/ToolTuner"
```

---

## Phase 1: 核心实现（四条线并行）

### 工作线 A: 多模态 Agent

### Task 5: ReAct 循环多模态消息转换

**Files:**
- Modify: `internal/agent/react_convert.go`
- Create: `internal/agent/react_convert_multimodal_test.go`

当前 `convertToLLMMessages` 将多模态内容扁平化为 JSON 字符串。需改为检测多模态 Provider 并使用结构化 DTO。

- [ ] **Step 1: 写测试**

```go
// internal/agent/react_convert_multimodal_test.go
package agent

import (
    "testing"
    "agentprimordia/internal/agent/multimodal"
)

func TestConvertMultimodalMessage(t *testing.T) {
    msg := Message{
        Role: "user",
        ContentParts: []multimodal.ContentPart{
            {Type: "text", Text: "描述这张图"},
            {Type: "image_url", URL: "https://example.com/img.png", MIME: "image/png"},
        },
    }
    if !msg.HasMultimodal() {
        t.Fatal("should detect multimodal")
    }
    text := msg.TextContent()
    if text != "描述这张图" {
        t.Errorf("expected '描述这张图', got '%s'", text)
    }
}

func TestConvertTextOnlyMessage(t *testing.T) {
    msg := Message{Role: "user", Content: "hello"}
    if msg.HasMultimodal() {
        t.Error("plain text should not be multimodal")
    }
    text := msg.TextContent()
    if text != "hello" {
        t.Errorf("expected 'hello', got '%s'", text)
    }
}
```

- [ ] **Step 2: 运行测试确认通过**（现有 Message 已支持 HasMultimodal/TextContent）

```bash
cd agentprimordia && go test ./internal/agent/ -run TestConvertMultimodal -v
cd agentprimordia && go test ./internal/agent/ -run TestConvertTextOnly -v
```

- [ ] **Step 3: 提交**

```bash
git add internal/agent/react_convert_multimodal_test.go
git commit -m "test: ReAct 多模态消息转换验收测试"
```

### Task 6: 多模态内置工具

**Files:**
- Create: `internal/agent/multimodal/tools.go`
- Create: `internal/agent/multimodal/tools_test.go`

定义多模态内置工具注册函数。

- [ ] **Step 1: 写测试**

```go
// internal/agent/multimodal/tools_test.go
package multimodal

import "testing"

func TestBuiltinToolDefinitions(t *testing.T) {
    tools := BuiltinTools()
    if len(tools) == 0 {
        t.Fatal("should have builtin multimodal tools")
    }
    names := make(map[string]bool)
    for _, tool := range tools {
        if tool.Name == "" {
            t.Error("tool name should not be empty")
        }
        if names[tool.Name] {
            t.Errorf("duplicate tool name: %s", tool.Name)
        }
        names[tool.Name] = true
    }
    // 至少应有 image_describe
    if !names["image_describe"] {
        t.Error("should have image_describe tool")
    }
}
```

- [ ] **Step 2: 实现**

```go
// internal/agent/multimodal/tools.go
package multimodal

// ToolDefinition 工具定义（与 tools.Registry 兼容的轻量描述）
type ToolDefinition struct {
    Name        string `json:"name"`
    Description string `json:"description"`
    InputSchema string `json:"input_schema"` // JSON Schema 字符串
}

// BuiltinTools 返回多模态内置工具定义列表
func BuiltinTools() []ToolDefinition {
    return []ToolDefinition{
        {
            Name:        "image_describe",
            Description: "描述图像内容，返回图像的文字描述",
            InputSchema: `{"type":"object","properties":{"image_url":{"type":"string","description":"图像URL或base64数据"}},"required":["image_url"]}`,
        },
        {
            Name:        "audio_transcribe",
            Description: "将音频转为文字",
            InputSchema: `{"type":"object","properties":{"audio_url":{"type":"string","description":"音频URL或base64数据"},"language":{"type":"string","description":"语言代码"}},"required":["audio_url"]}`,
        },
        {
            Name:        "image_generate",
            Description: "根据文字描述生成图像",
            InputSchema: `{"type":"object","properties":{"prompt":{"type":"string","description":"图像描述"},"size":{"type":"string","description":"图像尺寸"}},"required":["prompt"]}`,
        },
    }
}
```

- [ ] **Step 3: 运行测试**

```bash
cd agentprimordia && go test ./internal/agent/multimodal/ -run TestBuiltinTools -v
```

- [ ] **Step 4: 提交**

```bash
git add internal/agent/multimodal/tools.go internal/agent/multimodal/tools_test.go
git commit -m "feat: 多模态内置工具定义——image_describe/audio_transcribe/image_generate"
```

### Task 7: 多模态 bench 题面

**Files:**
- Create: `bench/multimodal/tasks.json`

20 道多模态任务（10 视觉 QA + 5 音频转写 + 5 图文混合推理）。

- [ ] **Step 1: 创建题面文件**

```json
[
  {"id":"mm-001","kind":"visual_qa","task":"描述 images/logo.png 中的主要内容","expected":"logo","fixtures":[{"path":"images/logo.png","inline_b64":"iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg=="}]},
  {"id":"mm-002","kind":"visual_qa","task":"images/chart.png 中折线图的最高点数值是多少","expected":"100","fixtures":[{"path":"images/chart.png","inline_b64":"placeholder"}]},
  {"id":"mm-003","kind":"visual_qa","task":"图片 images/photo.jpg 中有几个人","expected":"3","fixtures":[{"path":"images/photo.jpg","inline_b64":"placeholder"}]},
  {"id":"mm-004","kind":"visual_qa","task":"读取 images/table.png 中的表格数据，第二行第三列的值是什么","expected":"42","fixtures":[{"path":"images/table.png","inline_b64":"placeholder"}]},
  {"id":"mm-005","kind":"visual_qa","task":"images/sign.png 中的文字内容是什么","expected":"出口","fixtures":[{"path":"images/sign.png","inline_b64":"placeholder"}]},
  {"id":"mm-006","kind":"visual_qa","task":"images/map.png 中标记的地点名称是什么","expected":"公园","fixtures":[{"path":"images/map.png","inline_b64":"placeholder"}]},
  {"id":"mm-007","kind":"visual_qa","task":"images/code.png 中截图的编程语言是什么","expected":"python","fixtures":[{"path":"images/code.png","inline_b64":"placeholder"}]},
  {"id":"mm-008","kind":"visual_qa","task":"images/meme.png 图片表达了什么情绪","expected":"开心","fixtures":[{"path":"images/meme.png","inline_b64":"placeholder"}]},
  {"id":"mm-009","kind":"visual_qa","task":"images/product.png 中产品的颜色是什么","expected":"蓝色","fixtures":[{"path":"images/product.png","inline_b64":"placeholder"}]},
  {"id":"mm-010","kind":"visual_qa","task":"images/document.png 中的日期是什么","expected":"2026-09-05","fixtures":[{"path":"images/document.png","inline_b64":"placeholder"}]},
  {"id":"mm-011","kind":"audio_transcribe","task":"转写 audio/greeting.wav 的内容","expected":"你好世界","fixtures":[{"path":"audio/greeting.wav","inline_b64":"placeholder"}]},
  {"id":"mm-012","kind":"audio_transcribe","task":"audio/meeting.mp3 中提到了几个项目名称","expected":"3","fixtures":[{"path":"audio/meeting.mp3","inline_b64":"placeholder"}]},
  {"id":"mm-013","kind":"audio_transcribe","task":"转写 audio/number.wav 中念出的数字序列","expected":"12345","fixtures":[{"path":"audio/number.wav","inline_b64":"placeholder"}]},
  {"id":"mm-014","kind":"audio_transcribe","task":"audio/command.wav 中的指令是什么动作","expected":"打开文件","fixtures":[{"path":"audio/command.wav","inline_b64":"placeholder"}]},
  {"id":"mm-015","kind":"audio_transcribe","task":"audio/song.wav 歌词中重复最多的词是什么","expected":"爱","fixtures":[{"path":"audio/song.wav","inline_b64":"placeholder"}]},
  {"id":"mm-016","kind":"mixed_reasoning","task":"根据 images/pipeline.png 的流程图和 data/config.json 的配置，最终输出值是什么","expected":"pass","fixtures":[{"path":"images/pipeline.png","inline_b64":"placeholder"},{"path":"data/config.json","inline":"{\"threshold\": 50, \"value\": 75}"}]},
  {"id":"mm-017","kind":"mixed_reasoning","task":"对比 images/before.png 和 images/after.png，列出 3 个主要变化","expected":"变化","fixtures":[{"path":"images/before.png","inline_b64":"placeholder"},{"path":"images/after.png","inline_b64":"placeholder"}]},
  {"id":"mm-018","kind":"mixed_reasoning","task":"根据 images/architecture.png 的架构图和 README.md 的描述，系统有几个微服务","expected":"4","fixtures":[{"path":"images/architecture.png","inline_b64":"placeholder"},{"path":"README.md","inline":"# 系统架构\n本系统包含用户服务、订单服务、支付服务和通知服务四个微服务。"}]},
  {"id":"mm-019","kind":"mixed_reasoning","task":"images/error_screenshot.png 中的错误信息和 logs/app.log 最后 5 行的关键错误是否一致","expected":"一致","fixtures":[{"path":"images/error_screenshot.png","inline_b64":"placeholder"},{"path":"logs/app.log","inline":"2026-09-05 ERROR NullPointerException at line 42\n2026-09-05 INFO retry\n2026-09-05 ERROR NullPointerException at line 42\n2026-09-05 INFO giving up\n2026-09-05 ERROR crash\n"}]},
  {"id":"mm-020","kind":"mixed_reasoning","task":"根据 images/data_table.png 和 data/formula.txt 中的公式，计算结果写入 result.txt","expected":"result","fixtures":[{"path":"images/data_table.png","inline_b64":"placeholder"},{"path":"data/formula.txt","inline":"sum = a + b + c"}]}
]
```

- [ ] **Step 2: 提交**

```bash
git add bench/multimodal/tasks.json
git commit -m "feat: 多模态 bench 题面——20 道（10 视觉 QA + 5 音频 + 5 混合推理）"
```

### Task 8: 多模态 bench 运行器

**Files:**
- Create: `bench/multimodal/main.go`

A/B 实验运行器。A 臂=纯文本（图像转 OCR），B 臂=原生多模态。

- [ ] **Step 1: 实现运行器**（遵循 bench/gap/main.go 模式）

```go
// bench/multimodal/main.go
package main

import (
    "context"
    "encoding/json"
    "flag"
    "fmt"
    "os"
    "path/filepath"
    "strings"
    "time"

    "agentprimordia/internal/agent"
    "agentprimordia/internal/agent/multimodal"
    "agentprimordia/internal/eval"
    "agentprimordia/internal/llm"
    "agentprimordia/internal/persist"
    "agentprimordia/internal/tools"
    "agentprimordia/internal/tools/builtin"
)

type mmTask struct {
    ID       string `json:"id"`
    Kind     string `json:"kind"`
    Task     string `json:"task"`
    Expected string `json:"expected"`
    Fixtures []struct {
        Path      string `json:"path"`
        InlineB64 string `json:"inline_b64,omitempty"`
        Inline    string `json:"inline,omitempty"`
    } `json:"fixtures"`
}

type unitResult struct {
    Item        string `json:"item"`
    Arm         string `json:"arm"`
    Success     bool   `json:"success"`
    Turns       int    `json:"turns"`
    Tools       int    `json:"tools"`
    DurationSec int    `json:"duration_sec"`
    Error       string `json:"error,omitempty"`
}

func main() {
    model := flag.String("model", "sensenova-6.8-flash-lite", "模型名")
    apiKey := flag.String("api-key", "", "API key")
    baseURL := flag.String("base-url", "https://token.sensenova.cn/v1", "API base URL")
    outDir := flag.String("out", "bench/results/multimodal", "输出目录")
    pace := flag.Duration("pace", 10*time.Second, "运行间隔")
    flag.Parse()

    if *apiKey == "" {
        fmt.Fprintln(os.Stderr, "需要 --api-key")
        os.Exit(1)
    }

    // 加载题面
    tasks, err := loadTasks("bench/multimodal/tasks.json")
    if err != nil {
        fmt.Fprintf(os.Stderr, "加载题面失败: %v\n", err)
        os.Exit(1)
    }

    // 创建输出目录
    os.MkdirAll(*outDir, 0o755)
    resultsPath := filepath.Join(*outDir, "results.jsonl")

    // 幂等续跑：加载已完成单元
    done := loadDone(resultsPath)

    // 创建 Provider
    provider := llm.NewOpenAIProvider(llm.Config{
        Provider: "openai",
        Model:    *model,
        APIKey:   *apiKey,
        BaseURL:  *baseURL,
    })

    ctx := context.Background()
    arms := []string{"A", "B"}

    for _, task := range tasks {
        for _, arm := range arms {
            key := task.ID + "/" + arm
            if done[key] {
                continue
            }
            fmt.Printf("[%s/%s] %s\n", arm, task.ID, task.Task[:40])
            result := runUnit(ctx, provider, task, arm)
            appendResult(resultsPath, result)
            if result.Error != "" {
                fmt.Printf("  错误: %s\n", result.Error)
            } else {
                fmt.Printf("  结果: success=%v turns=%d\n", result.Success, result.Turns)
            }
            time.Sleep(*pace)
        }
    }
    fmt.Println("完成")
}

func loadTasks(path string) ([]mmTask, error) {
    data, err := os.ReadFile(path)
    if err != nil {
        return nil, err
    }
    var tasks []mmTask
    return tasks, json.Unmarshal(data, &tasks)
}

func loadDone(path string) map[string]bool {
    done := make(map[string]bool)
    f, err := os.Open(path)
    if err != nil {
        return done
    }
    defer f.Close()
    dec := json.NewDecoder(f)
    for {
        var r unitResult
        if err := dec.Decode(&r); err != nil {
            break
        }
        if r.Error == "" {
            done[r.Item+"/"+r.Arm] = true
        }
    }
    return done
}

func runUnit(ctx context.Context, provider *llm.OpenAIProvider, task mmTask, arm string) unitResult {
    start := time.Now()

    // 创建沙箱
    sandbox, err := os.MkdirTemp("", "mm-"+task.ID+"-")
    if err != nil {
        return unitResult{Item: task.ID, Arm: arm, Error: err.Error()}
    }
    defer os.RemoveAll(sandbox)

    // 注入 fixtures
    for _, fix := range task.Fixtures {
        fullPath := filepath.Join(sandbox, fix.Path)
        os.MkdirAll(filepath.Dir(fullPath), 0o755)
        content := fix.Inline
        if content == "" {
            content = fix.InlineB64
        }
        os.WriteFile(fullPath, []byte(content), 0o644)
    }

    // 构建工具包
    reg := tools.NewRegistry()
    fs := builtin.NewFileSystem(sandbox)
    sh := builtin.NewShell(builtin.WithWorkDir(sandbox))
    reg.Register(fs)
    reg.Register(sh)

    // 检查点
    ckpt, _ := persist.NewSQLiteCheckpointStore(filepath.Join(sandbox, "checkpoint.db"))

    // A 臂：纯文本（图像信息通过 OCR 文本注入 system prompt）
    // B 臂：多模态（图像通过 ContentPart 传入）
    sysPrompt := "你是一个文件处理助手。工作目录: " + sandbox
    if arm == "A" {
        sysPrompt += "\n注意：图像文件无法直接查看，请使用 shell 工具尝试提取文本信息。"
    }

    ag := agent.NewAgent("mm-agent", sysPrompt, provider,
        agent.WithMaxTurns(15),
        agent.WithToolkit(reg),
        agent.WithCheckpointStore(ckpt),
        agent.WithSessionID(task.ID+"-"+arm),
    )

    var msg agent.Message
    if arm == "B" {
        // B 臂：构建多模态消息
        parts := []multimodal.ContentPart{{Type: "text", Text: task.Task}}
        for _, fix := range task.Fixtures {
            if strings.HasPrefix(fix.Path, "images/") || strings.HasPrefix(fix.Path, "audio/") {
                parts = append(parts, multimodal.ContentPart{
                    Type: "image_url",
                    Data: fix.InlineB64,
                    MIME: "image/png",
                })
            }
        }
        msg = agent.Message{Role: "user", Content: task.Task, ContentParts: parts}
    } else {
        msg = agent.UserMessage(task.Task)
    }

    resp, err := ag.Run(ctx, msg)
    if err != nil {
        return unitResult{Item: task.ID, Arm: arm, Error: err.Error(), DurationSec: int(time.Since(start).Seconds())}
    }

    // 判定：检查 result.txt 或响应中是否包含 expected
    success := false
    resultContent, _ := os.ReadFile(filepath.Join(sandbox, "result.txt"))
    combined := string(resultContent) + " " + resp.Content
    if strings.Contains(strings.ToLower(combined), strings.ToLower(task.Expected)) {
        success = true
    }

    return unitResult{
        Item:        task.ID,
        Arm:         arm,
        Success:     success,
        Turns:       resp.Turns,
        DurationSec: int(time.Since(start).Seconds()),
    }
}

func appendResult(path string, r unitResult) {
    f, _ := os.OpenFile(path, os.O_APPEND|os.O_CREATE|os.O_WRONLY, 0o644)
    defer f.Close()
    json.NewEncoder(f).Encode(r)
}
```

- [ ] **Step 2: 编译验证**

```bash
cd agentprimordia && go build ./bench/multimodal/
```

- [ ] **Step 3: 提交**

```bash
git add bench/multimodal/
git commit -m "feat: 多模态 A/B bench 运行器——A=纯文本 B=原生多模态"
```

### Task 9: 多模态 bench 运行脚本

**Files:**
- Create: `bench/multimodal/run.sh`

- [ ] **Step 1: 创建运行脚本**

```bash
#!/bin/bash
# 多模态 A/B 实验
set -euo pipefail
cd "$(dirname "$0")/../.."
go run ./bench/multimodal \
  --model "sensenova-6.8-flash-lite" \
  --base-url "https://token.sensenova.cn/v1" \
  --api-key "sk-IE84nyrP9MdJXleKAfAKGNiawk81sHZW" \
  --out bench/results/multimodal \
  "$@"
```

- [ ] **Step 2: 设置可执行权限**

```bash
chmod +x bench/multimodal/run.sh
```

- [ ] **Step 3: 提交**

```bash
git add bench/multimodal/run.sh
git commit -m "feat: 多模态 bench 运行脚本"
```

---

### 工作线 B: 规划能力增强

### Task 10: 计划状态机实现

**Files:**
- Create: `internal/agent/planning/plan_state.go`
- Create: `internal/agent/planning/plan_state_test.go`

- [ ] **Step 1: 写测试**

```go
// internal/agent/planning/plan_state_test.go
package planning

import (
    "testing"
    "time"
)

func TestPlanStateMachine_Transitions(t *testing.T) {
    mp := &ManagedPlan{
        Plan:      &Plan{Goal: "test"},
        State:     PlanStatePending,
        CreatedAt: time.Now(),
    }

    // pending → active
    if err := mp.Transition(PlanStateActive, "开始执行"); err != nil {
        t.Fatalf("pending→active should succeed: %v", err)
    }
    if mp.State != PlanStateActive {
        t.Errorf("expected active, got %s", mp.State)
    }

    // active → blocked
    if err := mp.Transition(PlanStateBlocked, "等待审批"); err != nil {
        t.Fatalf("active→blocked should succeed: %v", err)
    }

    // blocked → active
    if err := mp.Transition(PlanStateActive, "审批通过"); err != nil {
        t.Fatalf("blocked→active should succeed: %v", err)
    }

    // active → completed
    if err := mp.Transition(PlanStateCompleted, "全部完成"); err != nil {
        t.Fatalf("active→completed should succeed: %v", err)
    }

    // completed → anything should fail
    if err := mp.Transition(PlanStateActive, "retry"); err == nil {
        t.Error("completed→active should fail")
    }
}

func TestPlanStateMachine_FailedTerminal(t *testing.T) {
    mp := &ManagedPlan{Plan: &Plan{Goal: "test"}, State: PlanStatePending}
    mp.Transition(PlanStateActive, "start")
    mp.Transition(PlanStateFailed, "不可恢复错误")
    if mp.State != PlanStateFailed {
        t.Errorf("expected failed, got %s", mp.State)
    }
    // failed → anything should fail
    if err := mp.Transition(PlanStateActive, "retry"); err == nil {
        t.Error("failed→active should fail")
    }
}

func TestPlanStateMachine_History(t *testing.T) {
    mp := &ManagedPlan{Plan: &Plan{Goal: "test"}, State: PlanStatePending}
    mp.Transition(PlanStateActive, "start")
    mp.Transition(PlanStateCompleted, "done")
    if len(mp.History) != 2 {
        t.Errorf("expected 2 transitions, got %d", len(mp.History))
    }
    if mp.History[0].From != PlanStatePending || mp.History[0].To != PlanStateActive {
        t.Error("first transition should be pending→active")
    }
}
```

- [ ] **Step 2: 实现状态机**

```go
// internal/agent/planning/plan_state.go
package planning

import (
    "fmt"
    "time"
)

// Transition 状态转换
func (mp *ManagedPlan) Transition(to PlanState, reason string) error {
    // 终态不可转换
    if mp.State == PlanStateCompleted || mp.State == PlanStateFailed {
        return fmt.Errorf("plan in terminal state %s, cannot transition to %s", mp.State, to)
    }

    // 合法转换表
    valid := map[PlanState][]PlanState{
        PlanStatePending: {PlanStateActive, PlanStateFailed},
        PlanStateActive:  {PlanStateBlocked, PlanStateCompleted, PlanStateFailed},
        PlanStateBlocked: {PlanStateActive, PlanStateFailed},
    }

    allowed := false
    for _, s := range valid[mp.State] {
        if s == to {
            allowed = true
            break
        }
    }
    if !allowed {
        return fmt.Errorf("invalid transition %s → %s", mp.State, to)
    }

    mp.History = append(mp.History, PlanTransition{
        From: mp.State,
        To:   to,
        At:   time.Now(),
        Why:  reason,
    })
    mp.State = to
    mp.UpdatedAt = time.Now()
    return nil
}
```

- [ ] **Step 3: 运行测试**

```bash
cd agentprimordia && go test ./internal/agent/planning/ -run TestPlanStateMachine -v
```

- [ ] **Step 4: 提交**

```bash
git add internal/agent/planning/plan_state.go internal/agent/planning/plan_state_test.go
git commit -m "feat: 计划状态机——pending/active/blocked/completed/failed 转换+历史"
```

### Task 11: 动态重规划器

**Files:**
- Create: `internal/agent/planning/replanner.go`
- Create: `internal/agent/planning/replanner_test.go`

- [ ] **Step 1: 写测试**

```go
// internal/agent/planning/replanner_test.go
package planning

import (
    "context"
    "testing"
    "agentprimordia/internal/llm"
)

func TestLLMReplanner_ShouldReplan_OnSubtaskFailure(t *testing.T) {
    mockLLM := &mockLLMForPlan{
        completeFunc: func(ctx context.Context, req *llm.CompletionRequest) (*llm.CompletionResponse, error) {
            return &llm.CompletionResponse{Content: "是，子任务失败导致后续步骤无法执行"}, nil
        },
    }
    r := NewLLMReplanner(mockLLM)
    plan := &Plan{
        Goal: "test",
        SubTasks: []SubTask{
            {ID: "s1", Description: "step 1", Status: TaskCompleted},
            {ID: "s2", Description: "step 2", Status: TaskFailed},
        },
    }
    should, reason := r.ShouldReplan(context.Background(), plan, "step 2 执行失败")
    if !should {
        t.Error("should trigger replan on subtask failure")
    }
    if reason == "" {
        t.Error("should provide a reason")
    }
}

func TestLLMReplanner_ShouldReplan_NoNeed(t *testing.T) {
    mockLLM := &mockLLMForPlan{
        completeFunc: func(ctx context.Context, req *llm.CompletionRequest) (*llm.CompletionResponse, error) {
            return &llm.CompletionResponse{Content: "否，当前计划正常执行中"}, nil
        },
    }
    r := NewLLMReplanner(mockLLM)
    plan := &Plan{
        Goal: "test",
        SubTasks: []SubTask{
            {ID: "s1", Description: "step 1", Status: TaskCompleted},
            {ID: "s2", Description: "step 2", Status: TaskRunning},
        },
    }
    should, _ := r.ShouldReplan(context.Background(), plan, "正常进展")
    if should {
        t.Error("should not trigger replan when plan is on track")
    }
}

// mockLLMForPlan 测试用 LLM mock
type mockLLMForPlan struct {
    completeFunc func(context.Context, *llm.CompletionRequest) (*llm.CompletionResponse, error)
}

func (m *mockLLMForPlan) Complete(ctx context.Context, req *llm.CompletionRequest) (*llm.CompletionResponse, error) {
    return m.completeFunc(ctx, req)
}
func (m *mockLLMForPlan) Stream(ctx context.Context, req *llm.CompletionRequest) (<-chan llm.Chunk, error) {
    return nil, nil
}
func (m *mockLLMForPlan) CallTools(ctx context.Context, req *llm.ToolCallRequest) (*llm.ToolCallResponse, error) {
    return nil, nil
}
func (m *mockLLMForPlan) Info() llm.ModelInfo { return llm.ModelInfo{} }
```

- [ ] **Step 2: 实现**

```go
// internal/agent/planning/replanner.go
package planning

import (
    "context"
    "fmt"
    "strings"

    "agentprimordia/internal/llm"
)

type LLMReplanner struct {
    provider llm.Provider
}

func NewLLMReplanner(provider llm.Provider) *LLMReplanner {
    return &LLMReplanner{provider: provider}
}

func (r *LLMReplanner) ShouldReplan(ctx context.Context, plan *Plan, observation string) (bool, string) {
    // 构建状态摘要
    var statusLines []string
    for _, st := range plan.SubTasks {
        statusLines = append(statusLines, fmt.Sprintf("  %s: %s", st.ID, st.Status))
    }
    statusSummary := strings.Join(statusLines, "\n")

    prompt := fmt.Sprintf(`你是一个任务规划器。当前计划状态：
目标: %s
子任务状态:
%s

最新观察: %s

请判断是否需要重新规划。只回答"是，<原因>"或"否，<原因>"。`, plan.Goal, statusSummary, observation)

    resp, err := r.provider.Complete(ctx, &llm.CompletionRequest{
        Messages:    []llm.ChatMessage{{Role: "user", Content: prompt}},
        MaxTokens:   100,
        Temperature: 0,
    })
    if err != nil {
        return false, "replanner error: " + err.Error()
    }

    content := strings.TrimSpace(resp.Content)
    if strings.HasPrefix(content, "是") {
        return true, content
    }
    return false, content
}

func (r *LLMReplanner) Replan(ctx context.Context, plan *Plan, reason string) (*Plan, error) {
    var statusLines []string
    for _, st := range plan.SubTasks {
        statusLines = append(statusLines, fmt.Sprintf("  %s [%s]: %s (结果: %s)", st.ID, st.Status, st.Description, st.Result))
    }

    prompt := fmt.Sprintf(`原计划失败，需要重新规划。
原目标: %s
原计划:
%s
失败原因: %s

请输出新的子任务分解，JSON 数组格式：[{"id":"s1","description":"...","depends_on":[]}]`,
        plan.Goal, strings.Join(statusLines, "\n"), reason)

    resp, err := r.provider.Complete(ctx, &llm.CompletionRequest{
        Messages:    []llm.ChatMessage{{Role: "user", Content: prompt}},
        MaxTokens:   500,
        Temperature: 0,
    })
    if err != nil {
        return nil, fmt.Errorf("replan LLM call failed: %w", err)
    }

    // 复用现有 Decompose 的 JSON 解析逻辑
    subTasks, err := parseSubTasks(resp.Content)
    if err != nil {
        return nil, fmt.Errorf("replan parse failed: %w", err)
    }

    return &Plan{Goal: plan.Goal, SubTasks: subTasks}, nil
}

// parseSubTasks 从 LLM 响应中解析子任务 JSON
func parseSubTasks(content string) ([]SubTask, error) {
    // 复用 planner.go 中的解析逻辑
    p := &LLMPlanner{}
    return p.parseSubTasksFromJSON(content)
}
```

- [ ] **Step 3: 需要重构 planner.go 暴露 parseSubTasksFromJSON**

在 `planner.go` 中将 JSON 解析逻辑提取为 `parseSubTasksFromJSON` 方法供 replanner 复用。

- [ ] **Step 4: 运行测试**

```bash
cd agentprimordia && go test ./internal/agent/planning/ -run TestLLMReplanner -v
```

- [ ] **Step 5: 提交**

```bash
git add internal/agent/planning/replanner.go internal/agent/planning/replanner_test.go internal/agent/planning/planner.go
git commit -m "feat: 动态重规划器——LLM 判断是否需要重规划 + 重新分解"
```

### Task 12: 规划失败恢复

**Files:**
- Create: `internal/agent/planning/recovery.go`
- Create: `internal/agent/planning/recovery_test.go`

- [ ] **Step 1: 写测试**

```go
// internal/agent/planning/recovery_test.go
package planning

import (
    "context"
    "testing"
    "agentprimordia/internal/llm"
)

func TestDeadlockDetector_ConsecutiveFailures(t *testing.T) {
    d := NewDeadlockDetector(3)
    plan := &Plan{Goal: "test", SubTasks: []SubTask{
        {ID: "s1", Status: TaskFailed},
        {ID: "s2", Status: TaskFailed},
        {ID: "s3", Status: TaskFailed},
    }}
    if !d.DetectDeadlock(context.Background(), plan, "s3") {
        t.Error("3 consecutive failures should trigger deadlock")
    }
}

func TestDeadlockDetector_BelowThreshold(t *testing.T) {
    d := NewDeadlockDetector(3)
    plan := &Plan{Goal: "test", SubTasks: []SubTask{
        {ID: "s1", Status: TaskFailed},
        {ID: "s2", Status: TaskCompleted},
        {ID: "s3", Status: TaskFailed},
    }}
    if d.DetectDeadlock(context.Background(), plan, "s3") {
        t.Error("non-consecutive failures should not trigger deadlock")
    }
}

func TestRecoveryStrategy_Recover(t *testing.T) {
    mockLLM := &mockLLMForPlan{
        completeFunc: func(ctx context.Context, req *llm.CompletionRequest) (*llm.CompletionResponse, error) {
            return &llm.CompletionResponse{Content: `[{"id":"s3","description":"替代方案：使用不同方法","depends_on":["s1"]}]`}, nil
        },
    }
    r := NewLLMRecoveryStrategy(mockLLM)
    plan := &Plan{Goal: "test", SubTasks: []SubTask{
        {ID: "s1", Status: TaskCompleted},
        {ID: "s2", Status: TaskFailed},
    }}
    recovered, err := r.Recover(context.Background(), plan, "s2")
    if err != nil {
        t.Fatalf("recover should succeed: %v", err)
    }
    if len(recovered.SubTasks) == 0 {
        t.Error("should have recovery subtasks")
    }
}
```

- [ ] **Step 2: 实现**

```go
// internal/agent/planning/recovery.go
package planning

import (
    "context"
    "fmt"
    "strings"

    "agentprimordia/internal/llm"
)

// DeadlockDetector 死路检测器——连续 N 个子任务失败视为死锁
type DeadlockDetector struct {
    threshold int
}

func NewDeadlockDetector(threshold int) *DeadlockDetector {
    if threshold < 2 {
        threshold = 2
    }
    return &DeadlockDetector{threshold: threshold}
}

func (d *DeadlockDetector) DetectDeadlock(ctx context.Context, plan *Plan, failedSubTask string) bool {
    // 检查从末尾往前是否有连续 threshold 个失败
    consecutive := 0
    for i := len(plan.SubTasks) - 1; i >= 0; i-- {
        if plan.SubTasks[i].Status == TaskFailed {
            consecutive++
        } else {
            break
        }
    }
    return consecutive >= d.threshold
}

// LLMRecoveryStrategy 基于 LLM 的恢复策略
type LLMRecoveryStrategy struct {
    provider llm.Provider
}

func NewLLMRecoveryStrategy(provider llm.Provider) *LLMRecoveryStrategy {
    return &LLMRecoveryStrategy{provider: provider}
}

func (r *LLMRecoveryStrategy) Recover(ctx context.Context, plan *Plan, failedSubTask string) (*Plan, error) {
    var context_lines []string
    for _, st := range plan.SubTasks {
        context_lines = append(context_lines, fmt.Sprintf("  %s [%s]: %s", st.ID, st.Status, st.Description))
    }

    prompt := fmt.Sprintf(`任务执行陷入死路，需要替代方案。
目标: %s
当前计划:
%s
失败子任务: %s

请输出替代子任务，JSON 数组格式：[{"id":"...","description":"...","depends_on":[...]}]`,
        plan.Goal, strings.Join(context_lines, "\n"), failedSubTask)

    resp, err := r.provider.Complete(ctx, &llm.CompletionRequest{
        Messages:    []llm.ChatMessage{{Role: "user", Content: prompt}},
        MaxTokens:   300,
        Temperature: 0,
    })
    if err != nil {
        return nil, err
    }

    subTasks, err := parseSubTasks(resp.Content)
    if err != nil {
        return nil, err
    }
    return &Plan{Goal: plan.Goal, SubTasks: subTasks}, nil
}
```

- [ ] **Step 3: 运行测试**

```bash
cd agentprimordia && go test ./internal/agent/planning/ -run TestDeadlock -v
cd agentprimordia && go test ./internal/agent/planning/ -run TestRecovery -v
```

- [ ] **Step 4: 提交**

```bash
git add internal/agent/planning/recovery.go internal/agent/planning/recovery_test.go
git commit -m "feat: 规划失败恢复——死路检测 + LLM 替代路径生成"
```

### Task 13: 审批门

**Files:**
- Create: `internal/agent/planning/approval.go`
- Create: `internal/agent/planning/approval_test.go`

- [ ] **Step 1: 写测试**

```go
// internal/agent/planning/approval_test.go
package planning

import (
    "context"
    "testing"
)

func TestPolicyApprovalGate_ConfiguredActions(t *testing.T) {
    gate := NewPolicyApprovalGate([]string{"shell_exec", "file_delete"})
    if !gate.RequiresApproval(context.Background(), "shell_exec") {
        t.Error("shell_exec should require approval")
    }
    if !gate.RequiresApproval(context.Background(), "file_delete") {
        t.Error("file_delete should require approval")
    }
    if gate.RequiresApproval(context.Background(), "file_read") {
        t.Error("file_read should not require approval")
    }
}

func TestPolicyApprovalGate_EmptyPolicy(t *testing.T) {
    gate := NewPolicyApprovalGate(nil)
    if gate.RequiresApproval(context.Background(), "anything") {
        t.Error("empty policy should not require approval")
    }
}
```

- [ ] **Step 2: 实现**

```go
// internal/agent/planning/approval.go
package planning

import (
    "context"
    "fmt"
    "sync"
)

// PolicyApprovalGate 基于策略列表的审批门
type PolicyApprovalGate struct {
    mu              sync.RWMutex
    requiredActions map[string]bool
    pendingApproval map[string]chan struct{}
}

func NewPolicyApprovalGate(actions []string) *PolicyApprovalGate {
    m := make(map[string]bool)
    for _, a := range actions {
        m[a] = true
    }
    return &PolicyApprovalGate{
        requiredActions: m,
        pendingApproval: make(map[string]chan struct{}),
    }
}

func (g *PolicyApprovalGate) RequiresApproval(ctx context.Context, action string) bool {
    g.mu.RLock()
    defer g.mu.RUnlock()
    return g.requiredActions[action]
}

func (g *PolicyApprovalGate) RequestApproval(ctx context.Context, action, reason string) error {
    g.mu.Lock()
    ch := make(chan struct{})
    g.pendingApproval[action] = ch
    g.mu.Unlock()
    // 实际部署中这里会发送通知给人类审批者
    // 当前实现：直接通过（测试模式）
    close(ch)
    return nil
}

func (g *PolicyApprovalGate) WaitApproval(ctx context.Context, action string) error {
    g.mu.RLock()
    ch, ok := g.pendingApproval[action]
    g.mu.RUnlock()
    if !ok {
        return nil // 无需审批
    }
    select {
    case <-ch:
        g.mu.Lock()
        delete(g.pendingApproval, action)
        g.mu.Unlock()
        return nil
    case <-ctx.Done():
        return fmt.Errorf("approval wait cancelled: %w", ctx.Err())
    }
}
```

- [ ] **Step 3: 运行测试**

```bash
cd agentprimordia && go test ./internal/agent/planning/ -run TestPolicyApproval -v
```

- [ ] **Step 4: 提交**

```bash
git add internal/agent/planning/approval.go internal/agent/planning/approval_test.go
git commit -m "feat: 审批门——策略驱动的高风险动作审批"
```

### Task 14: 规划增强 bench 运行器

**Files:**
- Create: `bench/planning-v71/main.go`
- Create: `bench/planning-v71/run.sh`

复用 `docs/evals/long-horizon-v1.json` 题面，A 臂=无规划器，B 臂=增强规划器。

- [ ] **Step 1: 实现运行器**（遵循 bench/eval-v61/main.go 模式，但 B 臂使用增强规划器）

核心差异点：
- A 臂：`agent.NewAgent(...)` 不带 `WithPlanner`
- B 臂：`agent.NewAgent(..., agent.WithPlanner(enhancedPlanner))` 其中 enhancedPlanner 组合 Decomposer + Replanner + RecoveryStrategy

- [ ] **Step 2: 编译验证**

```bash
cd agentprimordia && go build ./bench/planning-v71/
```

- [ ] **Step 3: 创建运行脚本**

```bash
#!/bin/bash
set -euo pipefail
cd "$(dirname "$0")/../.."
go run ./bench/planning-v71 \
  --model "sensenova-6.8-flash-lite" \
  --base-url "https://token.sensenova.cn/v1" \
  --api-key "sk-IE84nyrP9MdJXleKAfAKGNiawk81sHZW" \
  --out bench/results/planning-v71 \
  "$@"
```

- [ ] **Step 4: 提交**

```bash
git add bench/planning-v71/
git commit -m "feat: 规划增强 A/B bench——A=无规划 B=增强规划器"
```

---

### 工作线 C: 可观测性升级

### Task 15: otel 包迁移

**Files:**
- Move: `internal/otel/*.go` → `internal/observability/export/otel/*.go`
- Create: `internal/otel/alias.go` (向后兼容 re-export)

- [ ] **Step 1: 创建目标目录并移动文件**

```bash
mkdir -p internal/observability/export/otel
# 移动所有 .go 文件（除 doc.go）
for f in internal/otel/*.go; do
    base=$(basename "$f")
    if [ "$base" != "doc.go" ]; then
        mv "$f" "internal/observability/export/otel/$base"
    fi
done
# 移动 procfs/ 子目录
mv internal/otel/procfs internal/observability/export/otel/procfs
# 移动 ebpf/ 子目录
mv internal/otel/ebpf internal/observability/export/otel/ebpf
```

- [ ] **Step 2: 更新所有移动文件的 package 声明**

```bash
# 更新 package 名
sed -i '' 's/^package otel$/package otel/' internal/observability/export/otel/*.go
# procfs 子目录保持不变
```

- [ ] **Step 3: 创建向后兼容别名**

```go
// internal/otel/alias.go
// Package otel 已迁移至 internal/observability/export/otel
// 本文件提供向后兼容的类型别名
package otel

import (
    obsotel "agentprimordia/internal/observability/export/otel"
)

// 类型别名——向后兼容
type OTelBridge = obsotel.OTelBridge
type EnhancedBridge = obsotel.EnhancedBridge
type TelemetryProvider = obsotel.TelemetryProvider
type OTLPExporter = obsotel.OTLPExporter
type MetricExporter = obsotel.MetricExporter
```

- [ ] **Step 4: 更新 import 路径**

```bash
# 查找所有引用旧路径的文件
grep -rl 'internal/otel"' internal/ --include="*.go" | grep -v 'internal/otel/'
# 逐个更新 import 路径（otel 的直接使用者改为 observability/export/otel）
```

- [ ] **Step 5: 编译验证**

```bash
cd agentprimordia && go build ./...
```

- [ ] **Step 6: 运行测试**

```bash
cd agentprimordia && go test ./internal/observability/... -v -count=1
```

- [ ] **Step 7: 提交**

```bash
git add -A
git commit -m "refactor: otel 迁移至 observability/export/otel——统一可观测性包结构"
```

### Task 16: 告警引擎实现

**Files:**
- Create: `internal/observability/alert.go`
- Create: `internal/observability/alert_engine_test.go`

- [ ] **Step 1: 写测试**

```go
// internal/observability/alert_engine_test.go
package observability

import (
    "testing"
    "time"
)

func TestAlertEngine_Evaluate(t *testing.T) {
    store := NewCorrelationStore()
    engine := NewAlertEngine(store)

    // 注册规则：错误率 > 0.5
    engine.RegisterRule(&thresholdRule{
        name:      "high_error_rate",
        threshold: 0.5,
        severity:  SeverityCritical,
        metricFn: func(store *CorrelationStore) float64 {
            return 0.8 // 模拟 80% 错误率
        },
    })

    alerts := engine.Evaluate()
    if len(alerts) != 1 {
        t.Fatalf("expected 1 alert, got %d", len(alerts))
    }
    if alerts[0].Severity != SeverityCritical {
        t.Errorf("expected critical, got %s", alerts[0].Severity)
    }
}

func TestAlertEngine_NoAlertBelowThreshold(t *testing.T) {
    store := NewCorrelationStore()
    engine := NewAlertEngine(store)

    engine.RegisterRule(&thresholdRule{
        name:      "high_error_rate",
        threshold: 0.5,
        severity:  SeverityWarning,
        metricFn: func(store *CorrelationStore) float64 {
            return 0.2 // 20% < 50%
        },
    })

    alerts := engine.Evaluate()
    if len(alerts) != 0 {
        t.Errorf("expected 0 alerts, got %d", len(alerts))
    }
}

func TestAlertEngine_MultipleRules(t *testing.T) {
    store := NewCorrelationStore()
    engine := NewAlertEngine(store)

    engine.RegisterRule(&thresholdRule{
        name: "rule1", threshold: 0.5, severity: SeverityWarning,
        metricFn: func(s *CorrelationStore) float64 { return 0.8 },
    })
    engine.RegisterRule(&thresholdRule{
        name: "rule2", threshold: 1.0, severity: SeverityCritical,
        metricFn: func(s *CorrelationStore) float64 { return 0.3 },
    })

    alerts := engine.Evaluate()
    if len(alerts) != 1 {
        t.Fatalf("expected 1 alert (only rule1 triggers), got %d", len(alerts))
    }
    if alerts[0].Rule != "rule1" {
        t.Errorf("expected rule1, got %s", alerts[0].Rule)
    }
}
```

- [ ] **Step 2: 实现告警引擎**

```go
// internal/observability/alert.go
package observability

import "sync"

// AlertEngine 告警引擎——定期评估规则并产生告警
type AlertEngine struct {
    mu    sync.RWMutex
    store *CorrelationStore
    rules []AlertRule
}

func NewAlertEngine(store *CorrelationStore) *AlertEngine {
    return &AlertEngine{store: store}
}

func (e *AlertEngine) RegisterRule(rule AlertRule) {
    e.mu.Lock()
    defer e.mu.Unlock()
    e.rules = append(e.rules, rule)
}

func (e *AlertEngine) Evaluate() []AlertEvent {
    e.mu.RLock()
    rules := make([]AlertRule, len(e.rules))
    copy(rules, e.rules)
    e.mu.RUnlock()

    var alerts []AlertEvent
    for _, rule := range rules {
        events, err := rule.Evaluate(e.store)
        if err != nil {
            continue
        }
        alerts = append(alerts, events...)
    }
    return alerts
}
```

- [ ] **Step 3: 运行测试**

```bash
cd agentprimordia && go test ./internal/observability/ -run TestAlertEngine -v
```

- [ ] **Step 4: 提交**

```bash
git add internal/observability/alert.go internal/observability/alert_engine_test.go
git commit -m "feat: 告警引擎——规则注册 + 阈值评估 + 告警事件生成"
```

### Task 17: 仪表盘 API 扩展

**Files:**
- Modify: `internal/observability/http.go`
- Create: `internal/observability/dashboard_test.go`

扩展现有 HTTP API，新增告警查询、指标汇总、Agent 行为概览端点。

- [ ] **Step 1: 写测试**

```go
// internal/observability/dashboard_test.go
package observability

import (
    "encoding/json"
    "net/http"
    "net/http/httptest"
    "testing"
)

func TestDashboardAPI_Summary(t *testing.T) {
    store := NewCorrelationStore()
    store.Start("trace-1", "agent-1", "session-1")
    store.RecordLLM("trace-1", 100, 50, 30, 0.01)
    store.RecordTool("trace-1", 50)
    store.RecordTurn("trace-1")
    store.End("trace-1")

    engine := NewAlertEngine(store)
    handler := DashboardHandler(store, engine)

    req := httptest.NewRequest("GET", "/dashboard/summary", nil)
    w := httptest.NewRecorder()
    handler.ServeHTTP(w, req)

    if w.Code != http.StatusOK {
        t.Fatalf("expected 200, got %d", w.Code)
    }

    var summary map[string]interface{}
    json.NewDecoder(w.Body).Decode(&summary)
    if summary["total_traces"].(float64) != 1 {
        t.Errorf("expected 1 trace, got %v", summary["total_traces"])
    }
}

func TestDashboardAPI_Alerts(t *testing.T) {
    store := NewCorrelationStore()
    engine := NewAlertEngine(store)
    engine.RegisterRule(&thresholdRule{
        name: "test_rule", threshold: 0.5, severity: SeverityWarning,
        metricFn: func(s *CorrelationStore) float64 { return 0.8 },
    })

    handler := DashboardHandler(store, engine)
    req := httptest.NewRequest("GET", "/dashboard/alerts", nil)
    w := httptest.NewRecorder()
    handler.ServeHTTP(w, req)

    if w.Code != http.StatusOK {
        t.Fatalf("expected 200, got %d", w.Code)
    }
}
```

- [ ] **Step 2: 实现仪表盘处理器**

```go
// 在 http.go 中新增 DashboardHandler
func DashboardHandler(store *CorrelationStore, engine *AlertEngine) http.Handler {
    mux := http.NewServeMux()

    // 现有端点保留
    mux.Handle("/traces", Handler(store))

    // 新增仪表盘端点
    mux.HandleFunc("/dashboard/summary", func(w http.ResponseWriter, r *http.Request) {
        summary := map[string]interface{}{
            "total_traces":  store.Len(),
            "total_spans":   store.SpanCount(),
            "total_audits":  store.AuditCount(),
            "agents":        store.ListByAgent(),
        }
        w.Header().Set("Content-Type", "application/json")
        json.NewEncoder(w).Encode(summary)
    })

    mux.HandleFunc("/dashboard/alerts", func(w http.ResponseWriter, r *http.Request) {
        alerts := engine.Evaluate()
        w.Header().Set("Content-Type", "application/json")
        json.NewEncoder(w).Encode(alerts)
    })

    return mux
}
```

- [ ] **Step 3: 运行测试**

```bash
cd agentprimordia && go test ./internal/observability/ -run TestDashboard -v
```

- [ ] **Step 4: 提交**

```bash
git add internal/observability/http.go internal/observability/dashboard_test.go
git commit -m "feat: 仪表盘 API——/dashboard/summary + /dashboard/alerts"
```

### Task 18: 可观测性验收测试

**Files:**
- Create: `internal/observability/acceptance_test.go`

验收 spec §5.4 的 A1-A5 确定性断言。

- [ ] **Step 1: 写验收测试**

```go
// internal/observability/acceptance_test.go
package observability

import (
    "testing"
    "time"
)

// A1: trace 链路完整覆盖所有 ReAct 轮次和工具调用
func TestAcceptance_A1_TraceCompleteness(t *testing.T) {
    store := NewCorrelationStore()
    store.Start("t1", "agent1", "s1")
    // 模拟 3 轮 ReAct + 每轮 2 个工具调用
    for i := 0; i < 3; i++ {
        store.AddSpan("t1", SpanRecord{Name: "react.turn", Kind: "internal"})
        store.RecordTool("t1", 10)
        store.RecordTool("t1", 20)
        store.RecordTurn("t1")
    }
    store.End("t1")

    trace := store.Get("t1")
    if trace == nil {
        t.Fatal("trace should exist")
    }
    // 3 轮 × (1 turn span + 2 tool records) = 3 spans + 6 tool records + 3 turn records
    if len(trace.Spans) < 3 {
        t.Errorf("should have >= 3 spans (one per turn), got %d", len(trace.Spans))
    }
    if trace.Metrics.ToolCalls != 6 {
        t.Errorf("should have 6 tool calls, got %d", trace.Metrics.ToolCalls)
    }
    if trace.Metrics.Turns != 3 {
        t.Errorf("should have 3 turns, got %d", trace.Metrics.Turns)
    }
}

// A2: trace_id 可正向查询到所有关联审计事件
func TestAcceptance_A2_AuditCorrelation(t *testing.T) {
    store := NewCorrelationStore()
    store.Start("t1", "agent1", "s1")
    store.AddAuditEvent("t1", AuditEvent{
        Actor: "agent1", Action: "tool.call", Resource: "shell",
        Result: "success", Timestamp: time.Now(),
    })
    store.AddAuditEvent("t1", AuditEvent{
        Actor: "agent1", Action: "llm.call", Resource: "openai",
        Result: "success", Timestamp: time.Now(),
    })

    trace := store.Get("t1")
    if len(trace.AuditEvents) != 2 {
        t.Errorf("should have 2 audit events, got %d", len(trace.AuditEvents))
    }
    for _, e := range trace.AuditEvents {
        if e.TraceID != "t1" {
            t.Errorf("audit event trace_id should be t1, got %s", e.TraceID)
        }
    }
}

// A3: 告警规则触发后 < 5s 发出通知
func TestAcceptance_A3_AlertLatency(t *testing.T) {
    store := NewCorrelationStore()
    engine := NewAlertEngine(store)
    engine.RegisterRule(&thresholdRule{
        name: "latency_test", threshold: 0.1, severity: SeverityCritical,
        metricFn: func(s *CorrelationStore) float64 { return 1.0 },
    })

    start := time.Now()
    alerts := engine.Evaluate()
    elapsed := time.Since(start)

    if len(alerts) != 1 {
        t.Fatalf("expected 1 alert, got %d", len(alerts))
    }
    if elapsed > 5*time.Second {
        t.Errorf("alert evaluation took %v, should be < 5s", elapsed)
    }
}

// A4: 指标导出兼容 Prometheus scrape 格式
func TestAcceptance_A4_PrometheusFormat(t *testing.T) {
    store := NewCorrelationStore()
    store.Start("t1", "agent1", "s1")
    store.RecordLLM("t1", 100, 50, 30, 0.01)
    store.End("t1")

    trace := store.Get("t1")
    if trace.Metrics.LLMCalls != 1 {
        t.Errorf("should have 1 LLM call, got %d", trace.Metrics.LLMCalls)
    }
    if trace.Metrics.PromptTokens != 50 {
        t.Errorf("should have 50 prompt tokens, got %d", trace.Metrics.PromptTokens)
    }
}

// A5: 仪表盘 API 返回实时数据延迟 < 1s
func TestAcceptance_A5_DashboardLatency(t *testing.T) {
    store := NewCorrelationStore()
    // 填充一些数据
    for i := 0; i < 100; i++ {
        traceID := fmt.Sprintf("trace-%d", i)
        store.Start(traceID, "agent1", "s1")
        store.RecordLLM(traceID, 100, 50, 30, 0.01)
        store.End(traceID)
    }

    start := time.Now()
    traces := store.List(100)
    elapsed := time.Since(start)

    if len(traces) != 100 {
        t.Errorf("should list 100 traces, got %d", len(traces))
    }
    if elapsed > 1*time.Second {
        t.Errorf("dashboard query took %v, should be < 1s", elapsed)
    }
}
```

- [ ] **Step 2: 运行测试**

```bash
cd agentprimordia && go test ./internal/observability/ -run TestAcceptance -v
```

- [ ] **Step 3: 提交**

```bash
git add internal/observability/acceptance_test.go
git commit -m "test: 可观测性验收测试 A1-A5——trace 完整性/审计关联/告警延迟/Prometheus/仪表盘"
```

---

### 工作线 D: 工具智能统一

### Task 19: 工具目录 + 任务匹配

**Files:**
- Create: `internal/tools/intelligence/reuse/catalog.go`
- Create: `internal/tools/intelligence/reuse/catalog_test.go`
- Create: `internal/tools/intelligence/reuse/matcher.go`
- Create: `internal/tools/intelligence/reuse/matcher_test.go`

- [ ] **Step 1: 写目录测试**

```go
// internal/tools/intelligence/reuse/catalog_test.go
package reuse

import "testing"

func TestToolCatalog_RegisterAndList(t *testing.T) {
    c := NewToolCatalog()
    c.Register(ToolEntry{ID: "t1", Name: "shell", Description: "执行 shell 命令"})
    c.Register(ToolEntry{ID: "t2", Name: "filesystem", Description: "文件读写"})

    tools := c.List()
    if len(tools) != 2 {
        t.Errorf("expected 2 tools, got %d", len(tools))
    }
}

func TestToolCatalog_GetByID(t *testing.T) {
    c := NewToolCatalog()
    c.Register(ToolEntry{ID: "t1", Name: "shell", Description: "执行 shell 命令"})

    entry, ok := c.GetByID("t1")
    if !ok {
        t.Fatal("should find t1")
    }
    if entry.Name != "shell" {
        t.Errorf("expected shell, got %s", entry.Name)
    }

    _, ok = c.GetByID("nonexistent")
    if ok {
        t.Error("should not find nonexistent")
    }
}
```

- [ ] **Step 2: 实现目录**

```go
// internal/tools/intelligence/reuse/catalog.go
package reuse

import "sync"

type ToolEntry struct {
    ID          string `json:"id"`
    Name        string `json:"name"`
    Description string `json:"description"`
    Domain      string `json:"domain,omitempty"`
}

type ToolCatalog struct {
    mu    sync.RWMutex
    tools map[string]ToolEntry
}

func NewToolCatalog() *ToolCatalog {
    return &ToolCatalog{tools: make(map[string]ToolEntry)}
}

func (c *ToolCatalog) Register(entry ToolEntry) {
    c.mu.Lock()
    defer c.mu.Unlock()
    c.tools[entry.ID] = entry
}

func (c *ToolCatalog) GetByID(id string) (ToolEntry, bool) {
    c.mu.RLock()
    defer c.mu.RUnlock()
    e, ok := c.tools[id]
    return e, ok
}

func (c *ToolCatalog) List() []ToolEntry {
    c.mu.RLock()
    defer c.mu.RUnlock()
    result := make([]ToolEntry, 0, len(c.tools))
    for _, e := range c.tools {
        result = append(result, e)
    }
    return result
}
```

- [ ] **Step 3: 写匹配器测试**

```go
// internal/tools/intelligence/reuse/matcher_test.go
package reuse

import "testing"

func TestTaskMatcher_KeywordMatch(t *testing.T) {
    c := NewToolCatalog()
    c.Register(ToolEntry{ID: "t1", Name: "shell", Description: "执行 shell 命令 终端 命令行"})
    c.Register(ToolEntry{ID: "t2", Name: "filesystem", Description: "文件读写 目录 路径"})
    c.Register(ToolEntry{ID: "t3", Name: "csv_parser", Description: "CSV 解析 数据表格"})

    m := NewTaskMatcher()
    match := m.Match("计算 data.csv 中所有数值的平均值", c.List())
    if match.ID != "t3" {
        t.Errorf("expected csv_parser, got %s", match.Name)
    }
}

func TestTaskMatcher_NoMatch(t *testing.T) {
    c := NewToolCatalog()
    c.Register(ToolEntry{ID: "t1", Name: "shell", Description: "执行 shell 命令"})

    m := NewTaskMatcher()
    match := m.Match("生成一张图片", c.List())
    if match.ID != "" {
        t.Errorf("expected no match, got %s", match.Name)
    }
}
```

- [ ] **Step 4: 实现匹配器**

```go
// internal/tools/intelligence/reuse/matcher.go
package reuse

import "strings"

type TaskMatcher struct{}

func NewTaskMatcher() *TaskMatcher {
    return &TaskMatcher{}
}

// Match 基于关键词重叠的任务-工具匹配
func (m *TaskMatcher) Match(task string, tools []ToolEntry) ToolEntry {
    taskLower := strings.ToLower(task)
    var best ToolEntry
    bestScore := 0

    for _, tool := range tools {
        descLower := strings.ToLower(tool.Description)
        // 简单的关键词重叠计分
        score := 0
        for _, word := range strings.Fields(descLower) {
            if len(word) > 1 && strings.Contains(taskLower, word) {
                score++
            }
        }
        if score > bestScore {
            bestScore = score
            best = tool
        }
    }

    if bestScore == 0 {
        return ToolEntry{} // 无匹配
    }
    return best
}
```

- [ ] **Step 5: 运行测试**

```bash
cd agentprimordia && go test ./internal/tools/intelligence/reuse/ -v
```

- [ ] **Step 6: 提交**

```bash
git add internal/tools/intelligence/reuse/
git commit -m "feat: 工具目录 + 任务匹配器——关键词重叠匹配"
```

### Task 20: 性能画像器

**Files:**
- Create: `internal/tools/intelligence/optimize/profiler.go`
- Create: `internal/tools/intelligence/optimize/profiler_test.go`

- [ ] **Step 1: 写测试**

```go
// internal/tools/intelligence/optimize/profiler_test.go
package optimize

import (
    "context"
    "testing"
    "time"
    "agentprimordia/internal/tools/intelligence"
)

func TestProfiler_RecordAndProfile(t *testing.T) {
    p := NewInMemoryProfiler()
    ctx := context.Background()

    // 记录 5 次工具使用
    for i := 0; i < 5; i++ {
        p.Record(ctx, intelligence.ToolUsageRecord{
            ToolName: "shell",
            Success:  i < 4, // 80% 成功率
            Duration: time.Duration(100+i*10) * time.Millisecond,
            Tokens:   50,
        })
    }

    profile, err := p.Profile(ctx, "shell")
    if err != nil {
        t.Fatalf("profile error: %v", err)
    }
    if profile.TotalCalls != 5 {
        t.Errorf("expected 5 calls, got %d", profile.TotalCalls)
    }
    if profile.SuccessRate != 0.8 {
        t.Errorf("expected 0.8 success rate, got %f", profile.SuccessRate)
    }
}

func TestProfiler_AllProfiles(t *testing.T) {
    p := NewInMemoryProfiler()
    ctx := context.Background()

    p.Record(ctx, intelligence.ToolUsageRecord{ToolName: "shell", Success: true, Duration: time.Millisecond})
    p.Record(ctx, intelligence.ToolUsageRecord{ToolName: "filesystem", Success: true, Duration: time.Millisecond})

    profiles, err := p.AllProfiles(ctx)
    if err != nil {
        t.Fatalf("error: %v", err)
    }
    if len(profiles) != 2 {
        t.Errorf("expected 2 profiles, got %d", len(profiles))
    }
}
```

- [ ] **Step 2: 实现**

```go
// internal/tools/intelligence/optimize/profiler.go
package optimize

import (
    "context"
    "sort"
    "sync"
    "time"
    "agentprimordia/internal/tools/intelligence"
)

type InMemoryProfiler struct {
    mu      sync.Mutex
    records map[string][]intelligence.ToolUsageRecord
}

func NewInMemoryProfiler() *InMemoryProfiler {
    return &InMemoryProfiler{records: make(map[string][]intelligence.ToolUsageRecord)}
}

func (p *InMemoryProfiler) Record(ctx context.Context, usage intelligence.ToolUsageRecord) error {
    p.mu.Lock()
    defer p.mu.Unlock()
    p.records[usage.ToolName] = append(p.records[usage.ToolName], usage)
    return nil
}

func (p *InMemoryProfiler) Profile(ctx context.Context, toolName string) (*intelligence.ToolProfile, error) {
    p.mu.Lock()
    records := p.records[toolName]
    p.mu.Unlock()

    if len(records) == 0 {
        return &intelligence.ToolProfile{ToolName: toolName}, nil
    }

    profile := &intelligence.ToolProfile{
        ToolName:   toolName,
        TotalCalls: len(records),
    }

    successes := 0
    var totalDuration time.Duration
    durations := make([]time.Duration, 0, len(records))
    totalTokens := 0

    for _, r := range records {
        if r.Success {
            successes++
        }
        totalDuration += r.Duration
        durations = append(durations, r.Duration)
        totalTokens += r.Tokens
        if r.Timestamp.After(profile.LastUsed) {
            profile.LastUsed = r.Timestamp
        }
    }

    profile.SuccessRate = float64(successes) / float64(len(records))
    profile.AvgDuration = totalDuration / time.Duration(len(records))
    profile.AvgTokens = totalTokens / len(records)

    sort.Slice(durations, func(i, j int) bool { return durations[i] < durations[j] })
    p95Idx := int(float64(len(durations)) * 0.95)
    if p95Idx >= len(durations) {
        p95Idx = len(durations) - 1
    }
    profile.P95Duration = durations[p95Idx]

    return profile, nil
}

func (p *InMemoryProfiler) AllProfiles(ctx context.Context) (map[string]*intelligence.ToolProfile, error) {
    p.mu.Lock()
    names := make([]string, 0, len(p.records))
    for name := range p.records {
        names = append(names, name)
    }
    p.mu.Unlock()

    result := make(map[string]*intelligence.ToolProfile)
    for _, name := range names {
        profile, _ := p.Profile(ctx, name)
        result[name] = profile
    }
    return result, nil
}
```

- [ ] **Step 3: 运行测试**

```bash
cd agentprimordia && go test ./internal/tools/intelligence/optimize/ -run TestProfiler -v
```

- [ ] **Step 4: 提交**

```bash
git add internal/tools/intelligence/optimize/profiler.go internal/tools/intelligence/optimize/profiler_test.go
git commit -m "feat: 工具性能画像器——per-tool 成功率/延迟/token 统计 + P95"
```

### Task 21: 参数调优器 + 工具选择器

**Files:**
- Create: `internal/tools/intelligence/optimize/tuner.go`
- Create: `internal/tools/intelligence/optimize/tuner_test.go`
- Create: `internal/tools/intelligence/optimize/selector.go`
- Create: `internal/tools/intelligence/optimize/selector_test.go`

- [ ] **Step 1: 写调优器测试**

```go
// internal/tools/intelligence/optimize/tuner_test.go
package optimize

import (
    "context"
    "testing"
    "agentprimordia/internal/tools/intelligence"
)

func TestDataDrivenTuner_SuggestTuning(t *testing.T) {
    tuner := NewDataDrivenTuner()
    profile := &intelligence.ToolProfile{
        ToolName:    "shell",
        TotalCalls:  20,
        SuccessRate: 0.6,
        AvgDuration: 5 * time.Second,
    }

    suggestion, err := tuner.SuggestTuning(context.Background(), "shell", profile)
    if err != nil {
        t.Fatalf("error: %v", err)
    }
    // 成功率低于 0.7 应该给出调优建议
    if suggestion == nil {
        t.Fatal("should suggest tuning for low success rate")
    }
    if suggestion.Confidence <= 0 {
        t.Error("confidence should be positive")
    }
}

func TestDataDrivenTuner_NoTuningNeeded(t *testing.T) {
    tuner := NewDataDrivenTuner()
    profile := &intelligence.ToolProfile{
        ToolName:    "shell",
        TotalCalls:  20,
        SuccessRate: 0.95,
    }

    suggestion, _ := tuner.SuggestTuning(context.Background(), "shell", profile)
    if suggestion != nil {
        t.Error("should not suggest tuning for high success rate")
    }
}
```

- [ ] **Step 2: 实现调优器**

```go
// internal/tools/intelligence/optimize/tuner.go
package optimize

import (
    "context"
    "fmt"
    "time"
    "agentprimordia/internal/tools/intelligence"
)

type DataDrivenTuner struct{}

func NewDataDrivenTuner() *DataDrivenTuner {
    return &DataDrivenTuner{}
}

func (t *DataDrivenTuner) SuggestTuning(ctx context.Context, toolName string, profile *intelligence.ToolProfile) (*intelligence.TuningSuggestion, error) {
    if profile.SuccessRate >= 0.7 {
        return nil, nil // 成功率足够高，无需调优
    }

    // 基于性能画像数据驱动建议
    if profile.AvgDuration > 3*time.Second {
        return &intelligence.TuningSuggestion{
            ToolName:     toolName,
            Parameter:    "timeout",
            CurrentVal:   profile.AvgDuration.String(),
            SuggestedVal: (profile.AvgDuration * 2).String(),
            Confidence:   0.6,
            Reason:       fmt.Sprintf("平均延迟 %.1fs 较高，建议增加超时", profile.AvgDuration.Seconds()),
        }, nil
    }

    return &intelligence.TuningSuggestion{
        ToolName:     toolName,
        Parameter:    "retry",
        CurrentVal:   "0",
        SuggestedVal: "2",
        Confidence:   1.0 - profile.SuccessRate,
        Reason:       fmt.Sprintf("成功率 %.0f%% 偏低，建议增加重试次数", profile.SuccessRate*100),
    }, nil
}

func (t *DataDrivenTuner) ApplyTuning(ctx context.Context, toolName string, suggestion *intelligence.TuningSuggestion) error {
    // 当前为记录模式——实际调优需要与工具注册表集成
    return nil
}
```

- [ ] **Step 3: 写选择器测试**

```go
// internal/tools/intelligence/optimize/selector_test.go
package optimize

import (
    "context"
    "testing"
)

func TestHistorySelector_FirstCall(t *testing.T) {
    s := NewHistorySelector()
    choice, err := s.Select(context.Background(), "计算 CSV 平均值", []string{"shell", "csv_parser", "filesystem"})
    if err != nil {
        t.Fatalf("error: %v", err)
    }
    // 首次调用无历史，返回第一个候选
    if choice == "" {
        t.Error("should return a choice")
    }
}

func TestHistorySelector_LearnFromOutcomes(t *testing.T) {
    s := NewHistorySelector()
    ctx := context.Background()

    // 记录 csv_parser 成功 3 次
    for i := 0; i < 3; i++ {
        s.RecordOutcome(ctx, "csv_parser", true)
    }
    // 记录 shell 失败 2 次
    for i := 0; i < 2; i++ {
        s.RecordOutcome(ctx, "shell", false)
    }

    choice, _ := s.Select(ctx, "any task", []string{"shell", "csv_parser"})
    if choice != "csv_parser" {
        t.Errorf("expected csv_parser (higher success rate), got %s", choice)
    }
}
```

- [ ] **Step 4: 实现选择器**

```go
// internal/tools/intelligence/optimize/selector.go
package optimize

import (
    "context"
    "sync"
)

type HistorySelector struct {
    mu      sync.Mutex
    success map[string]int
    total   map[string]int
}

func NewHistorySelector() *HistorySelector {
    return &HistorySelector{
        success: make(map[string]int),
        total:   make(map[string]int),
    }
}

func (s *HistorySelector) Select(ctx context.Context, task string, candidates []string) (string, error) {
    s.mu.Lock()
    defer s.mu.Unlock()

    if len(candidates) == 0 {
        return "", nil
    }

    // 选历史成功率最高的
    bestChoice := candidates[0]
    bestRate := -1.0

    for _, c := range candidates {
        total := s.total[c]
        if total == 0 {
            continue
        }
        rate := float64(s.success[c]) / float64(total)
        if rate > bestRate {
            bestRate = rate
            bestChoice = c
        }
    }
    return bestChoice, nil
}

func (s *HistorySelector) RecordOutcome(ctx context.Context, toolName string, success bool) error {
    s.mu.Lock()
    defer s.mu.Unlock()
    s.total[toolName]++
    if success {
        s.success[toolName]++
    }
    return nil
}
```

- [ ] **Step 5: 运行测试**

```bash
cd agentprimordia && go test ./internal/tools/intelligence/optimize/ -v
```

- [ ] **Step 6: 提交**

```bash
git add internal/tools/intelligence/optimize/
git commit -m "feat: 参数调优器 + 工具选择器——数据驱动调优 + 历史成功率选择"
```

### Task 22: 缺口检测 + 工具生成（包装 lifecycle）

**Files:**
- Create: `internal/tools/intelligence/create/detector.go`
- Create: `internal/tools/intelligence/create/detector_test.go`
- Create: `internal/tools/intelligence/create/creator.go`

- [ ] **Step 1: 写检测器测试**

```go
// internal/tools/intelligence/create/detector_test.go
package create

import (
    "context"
    "testing"
    "agentprimordia/internal/tools/intelligence"
)

func TestGapDetector_MissingTool(t *testing.T) {
    d := NewTraceGapDetector()
    traces := []intelligence.ToolCallRecord{
        {ToolName: "", Error: "no suitable tool found for csv parsing", Success: false},
        {ToolName: "", Error: "no suitable tool found for csv parsing", Success: false},
        {ToolName: "", Error: "no suitable tool found for csv parsing", Success: false},
    }

    gaps, err := d.Detect(context.Background(), traces)
    if err != nil {
        t.Fatalf("error: %v", err)
    }
    if len(gaps) == 0 {
        t.Fatal("should detect missing tool gap")
    }
    if gaps[0].Kind != "missing_tool" {
        t.Errorf("expected missing_tool, got %s", gaps[0].Kind)
    }
}

func TestGapDetector_NoGapOnSuccess(t *testing.T) {
    d := NewTraceGapDetector()
    traces := []intelligence.ToolCallRecord{
        {ToolName: "shell", Success: true},
        {ToolName: "filesystem", Success: true},
    }

    gaps, _ := d.Detect(context.Background(), traces)
    if len(gaps) != 0 {
        t.Errorf("should not detect gaps on success, got %d", len(gaps))
    }
}
```

- [ ] **Step 2: 实现检测器**

```go
// internal/tools/intelligence/create/detector.go
package create

import (
    "context"
    "strings"
    "sync"
    "time"
    "agentprimordia/internal/tools/intelligence"
)

type TraceGapDetector struct {
    mu sync.Mutex
}

func NewTraceGapDetector() *TraceGapDetector {
    return &TraceGapDetector{}
}

func (d *TraceGapDetector) Detect(ctx context.Context, trace []intelligence.ToolCallRecord) ([]intelligence.GapCandidate, error) {
    d.mu.Lock()
    defer d.mu.Unlock()

    // 统计失败模式
    failCounts := make(map[string]int)
    firstSeen := make(map[string]time.Time)
    lastSeen := make(map[string]time.Time)
    sampleErrors := make(map[string]string)

    for _, r := range trace {
        if r.Success {
            continue
        }
        // 从错误消息中提取缺口类型
        key := extractGapKey(r.Error)
        if key == "" {
            continue
        }
        failCounts[key]++
        if _, ok := firstSeen[key]; !ok {
            firstSeen[key] = r.Timestamp
        }
        lastSeen[key] = r.Timestamp
        sampleErrors[key] = r.Error
    }

    var gaps []intelligence.GapCandidate
    for key, count := range failCounts {
        if count >= 2 { // 至少 2 次失败才报告
            gaps = append(gaps, intelligence.GapCandidate{
                Kind:        "missing_tool",
                Key:         key,
                Count:       count,
                SampleError: sampleErrors[key],
                FirstSeen:   firstSeen[key],
                LastSeen:    lastSeen[key],
            })
        }
    }
    return gaps, nil
}

func extractGapKey(errorMsg string) string {
    if strings.Contains(errorMsg, "no suitable tool") {
        // 提取工具类型关键词
        for _, keyword := range []string{"csv", "json", "xml", "yaml", "image", "audio"} {
            if strings.Contains(errorMsg, keyword) {
                return keyword + "_parser"
            }
        }
        return "unknown_tool"
    }
    return ""
}
```

- [ ] **Step 3: 实现生成器（包装 lifecycle autoloop）**

```go
// internal/tools/intelligence/create/creator.go
package create

import (
    "context"
    "fmt"
    "agentprimordia/internal/tools/intelligence"
)

// LifecycleCreator 包装 tools/lifecycle 的 AutoLoop 链路
type LifecycleCreator struct {
    // 实际部署中引用 lifecycle.AutoLoop
}

func NewLifecycleCreator() *LifecycleCreator {
    return &LifecycleCreator{}
}

func (c *LifecycleCreator) Create(ctx context.Context, gap intelligence.GapCandidate) (*intelligence.ToolArtifact, error) {
    // 当前为桩实现——实际部署中调用 lifecycle.AutoLoop.RunCandidate
    return &intelligence.ToolArtifact{
        ID:          fmt.Sprintf("gap-%s-%s", gap.Kind, gap.Key),
        Name:        gap.Key,
        Description: fmt.Sprintf("自动生成的工具: %s", gap.SampleError),
    }, nil
}
```

- [ ] **Step 4: 运行测试**

```bash
cd agentprimordia && go test ./internal/tools/intelligence/create/ -v
```

- [ ] **Step 5: 提交**

```bash
git add internal/tools/intelligence/create/
git commit -m "feat: 缺口检测器 + 工具生成器——从失败轨迹识别缺口并生成工具"
```

### Task 23: 工具智能 bench 题面 + 运行器

**Files:**
- Create: `bench/intelligence/tasks.json`
- Create: `bench/intelligence/main.go`
- Create: `bench/intelligence/run.sh`

30 道工具密集型任务。A 臂=分离系统，B 臂=统一工具智能。

- [ ] **Step 1: 创建题面**（30 道任务，混合需要工具创建和工具优化的场景）

- [ ] **Step 2: 实现运行器**（遵循 bench/gap/main.go 模式）

核心差异：
- A 臂：使用 `tools/lifecycle` + `tool_learning` 分离入口
- B 臂：使用 `tools/intelligence` 统一入口

- [ ] **Step 3: 编译验证**

```bash
cd agentprimordia && go build ./bench/intelligence/
```

- [ ] **Step 4: 提交**

```bash
git add bench/intelligence/
git commit -m "feat: 工具智能 A/B bench——A=分离系统 B=统一工具智能"
```

---

## Phase 2: 集成与验收

### Task 24: ReAct 循环集成——规划 Hook

**Files:**
- Create: `internal/agent/planning/hooks.go`
- Modify: `internal/agent/react_loop_core.go`（集成增强规划器）

- [ ] **Step 1: 实现规划 Hook**

将 Replanner + RecoveryStrategy 集成到 `react_plan_executor.go` 的执行流程中：
- 子任务失败时调用 `Replanner.ShouldReplan()` 判断是否需要重规划
- 连续失败时调用 `DeadlockDetector.DetectDeadlock()` 检测死锁
- 死锁时调用 `RecoveryStrategy.Recover()` 生成替代方案

- [ ] **Step 2: 写集成测试**

- [ ] **Step 3: 运行全量测试**

```bash
cd agentprimordia && go test ./internal/agent/... -count=1 -timeout 120s
```

- [ ] **Step 4: 提交**

```bash
git add internal/agent/planning/hooks.go internal/agent/react_loop_core.go
git commit -m "feat: 规划增强集成——ReAct 循环接入重规划 + 失败恢复"
```

### Task 25: ReAct 循环集成——工具智能 Hook

**Files:**
- Create: `internal/tools/intelligence/hooks.go`

- [ ] **Step 1: 实现 Hook**

在 ReAct 循环的工具调用后：
1. 调用 `Profiler.Record()` 记录工具使用
2. 失败时调用 `GapDetector.Detect()` 检测缺口
3. 检测到缺口时自动触发 `Creator.Create()` + `AutoLoop`

- [ ] **Step 2: 运行测试**

```bash
cd agentprimordia && go test ./internal/tools/intelligence/... -v
```

- [ ] **Step 3: 提交**

```bash
git add internal/tools/intelligence/hooks.go
git commit -m "feat: 工具智能 ReAct Hook——使用后画像 + 失败后缺口检测"
```

### Task 26: 全量编译 + 测试

- [ ] **Step 1: 全量编译**

```bash
cd agentprimordia && go build ./...
```

- [ ] **Step 2: 全量测试**

```bash
cd agentprimordia && go test ./... -count=1 -timeout 300s
```

- [ ] **Step 3: 修复所有失败**

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "feat: v7.1-rc1 工程地板——五命题接口+核心算法+单测全通过"
```

### Task 27: 启动 bench 实验

- [ ] **Step 1: 启动 E-1 续跑**（后台）

```bash
cd agentprimordia && bash bench/eval-v61/run.sh &
```

- [ ] **Step 2: 启动 E-4 扩量**（后台）

```bash
cd agentprimordia && bash bench/gap/run.sh &
```

- [ ] **Step 3: 启动 E-5 扩量**（后台）

```bash
cd agentprimordia && bash bench/live/run.sh &
```

- [ ] **Step 4: 启动多模态 bench**（后台）

```bash
cd agentprimordia && bash bench/multimodal/run.sh &
```

- [ ] **Step 5: 启动规划增强 bench**（后台）

```bash
cd agentprimordia && bash bench/planning-v71/run.sh &
```

- [ ] **Step 6: 启动工具智能 bench**（后台）

```bash
cd agentprimordia && bash bench/intelligence/run.sh &
```

### Task 28: 验收报告

所有 bench 完成后：

- [ ] **Step 1: 汇总各实验 McNemar 结果**

```bash
# 每个 bench 输出 summary.json，包含 McNemar p-value 和差值点估计
for dir in v61 gap live multimodal planning-v71 intelligence; do
    echo "=== $dir ==="
    cat bench/results/$dir/summary.json | python3 -m json.tool
done
```

- [ ] **Step 2: 版本升级至 v7.1.0 GA**

- [ ] **Step 3: 提交发版**

```bash
git commit -m "feat: v7.1.0 GA——五命题验收通过"
```
