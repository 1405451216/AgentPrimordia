# 双线豁免矩阵（S0-5）

> **定位**：V7 弧线铁律 5（双语言同步）的执行台账。核心能力变更 Go/TS 对等落地；无法即时对等的条目在本表登记**书面豁免**，杜绝「沉默的不一致」。
> **规则**：
> 1. 每个弧线新能力立项时必须在本表登记一行：双线状态 = `对等` / `Go-only` / `TS-only` / `协议对等+运行时豁免`；
> 2. 豁免必须附**理由**与**升格条件**（何时、以何种形态补齐对侧）；
> 3. 本表进 CI 审查（`scripts/dual-line-matrix-check.mjs`，校验表内引用的模块路径真实存在）；
> 4. TS 侧以**协议/格式消费者**身份参与时记 `协议对等`——共享数据契约（签名格式、资产包、API wire format）必须双线一致，豁免仅针对**执行运行时**。

## 存量基线（v6.0 时点）

| # | 能力 | Go | TS | 状态 | 说明 |
|---|------|----|----|------|------|
| B1 | ReAct 引擎/策略内核 | `internal/agent/strategy/` | `src/agent/`（react-loop） | 对等 | 双线 parity 有 cross-language-spec 门 |
| B2 | 记忆（SQLite/向量/HNSW/固化） | `internal/memory/` | `src/memory/` | 对等 | HNSW ef-search 双线算法对齐（v5.1） |
| B3 | LLM Provider 抽象 | `internal/llm/` | `src/llm/` | 对等 | provider 集合差异为生态差异非架构差异 |
| B4 | WASM 沙箱执行 | wazero（`agentprimordia/wasm/`、根 `wasm/`） | **无等价运行时** | Go-only | TS 侧工具执行委托 gateway 服务节点（`gateway/`，HTTP/wasm 边界）；升格条件：WebAssembly.Global 原生 API 覆盖彩排语义评审 |
| B5 | 混沌真实注入（tc/iptables） | `chaos/real_injector_linux.go` | N/A（平台限制） | Go-only | 豁免永久有效——TS 侧以故障注入 Provider 对等 |

## 弧线登记（V7，随版本追加）

| # | 能力 | 版本 | Go | TS | 状态 | 理由与升格条件 |
|---|------|------|----|----|------|----------------|
| 1 | 世界模型（状态图/预演/回溯/state-checkpoint） | v6.1 | `internal/agent/worldmodel/` | `src/agent/worldmodel/` | 对等（2026-08-31 双线落地） | 图内核体量中小，无平台依赖，无豁免；确定性语义经 testdata/worldmodel_fixtures.json 跨语言对账（NodeID 位级/ComparePaths/Rehearse 文案/Snapshot JSON 互换） |
| 2 | 蒸馏管道（轨迹→数据集→影子评测→路由） | v6.2 | `internal/agent/learning/pipeline/` | `src/learning/pipeline.ts` | 协议对等+运行时豁免（2026-08-31 工程地板双线落地） | 数据集导出格式/影子评测协议/路由契约双线一致；训练连接器（微调端点调用）Go-only——TS 经同一 HTTP 契约消费工件；升格条件：TS 侧 connector 若生态有原生 SDK 需求再评 |
| 3 | 开放工具生命周期 | v6.3 | `internal/tools/lifecycle/` + wazero 彩排 | `src/tools/lifecycle.ts` | 协议对等+运行时豁免（2026-08-31 地板双线落地；code 通道待提案批准） | 六段生命周期、签名/工具包格式、注册客户端双线对等（TS 可注册/消费/退役）；**沙箱彩排执行 Go-only**（承 B4），TS 工具彩排委托 gateway 节点；升格条件同 B4 |
| 4 | 常驻运行时（长活） | v6.4 | `internal/agent/live/` + `cmd/ap live` | `src/agent/live/`（Node worker 长循环 + idle 调度） | 对等（2026-08-31 Go 端工程地板落地；TS 端随版本实施） | 事件循环/idle 调度为语言无关模式；宿主托管共用；14 天实测待 B2 宿主（降级豁免中） |
| 5 | 联邦社会（跨节点黑板/资产/信任层） | v6.5 | `internal/multi_agent/federation/` + `internal/agent/cluster/` | `src/federation/protocol.ts`（组织客户端+资产消费者） | 协议对等（2026-08-31 地板双线落地） | 黑板协议/联邦资产三形态/信任评分双线一致；etcd/gRPC 节点总线 Go-only（TS 经 A2A/HTTP 面接入）；升格条件：TS gRPC-web 生态评估入 v6.5 评审 |
| 6 | Embedding Provider（S0-3） | S0 | `internal/llm/embedding.go` | `src/llm/embedding.ts` | 对等 | 接口+双实现（本地/API）+降级伪向量，三线一致 |

> 维护：每版本验收时由该版负责人更新；v7.0 定型评审以本表为 API 转正清单之一。
