# AgentPrimordia 宣传视频提示词 v2（全仓事实核对版）

> v2 基于「README 全文 + 文档全集 + Go/TS 源码 + Studio」三路调查重写，取代 v1。所有数字均给出处；无法溯源的对比数据一律列入黑名单禁用。
> 适用工具：Sora / Veo 3 / Runway Gen-3 / 可灵 / Pika / 海螺。成片 75 秒，16:9，4K。
> **双轨制**：A 轨 = AI 生成概念动画（本提示词投喂视频模型，每幕 6~8 秒逐段生成）；B 轨 = 真实录屏（Studio / demo / CLI，见第七节）。两轨交替剪辑，概念与实证互证。

---

## 一、核心信息（定稿口径，全部经代码核实）

- **官方定位**（README.md:3）：通用 AI Agent 开发框架 —— 轻量、并发原生、生产验证
- **官方主标语**（CODE_WIKI.md:3）：**万物之源，智能之始** —— 与英文名 Primordia（原初）同构，片中英双语对出
- **行动 Slogan**：从一行代码，到生产级智能体。/ From one line of code, to production-grade agents.
- **版本口径**：v6.0.0（pkg/agent.go:655 与 VERSION / CHANGELOG / package.json 四方一致；README 首页 v5.0.0 徽章已过期，勿引用）
- **双 SDK**：Go（非测试 128,252 行 / 613 文件）+ TypeScript（src 51,328 行 / 46 模块，22 组对等映射全部 ✅）
- **引擎**：ReAct 循环 + **35 个生命周期钩子**（internal/agent/hooks/hooks.go:74-108）+ 每轮 checkpoint 断点续跑
- **编排**：Pipeline / Handoff / Parallel（agent/orchestration.go）、DAG（dag.go）、GroupChat（collaboration.go）、Swarm（multi_agent/swarm.go）、MapReduce、Debate、A2A
- **记忆**：SQLite FTS5 + HNSW 向量 + RAG，RRF 混合检索
- **模型**：**13 个 Provider 实现**（OpenAI / Anthropic / Azure / Gemini / GLM / Qwen / DeepSeek / Mistral / Cohere / Ollama + 多模态变体）+ Resilient 重试/降级/熔断
- **安全**：ACL / WASM 沙箱（wazero）/ Guardrails / PII Trie / 路径遍历与 symlink 防护 / AES-GCM 密钥 / 多租户配额
- **CLI**：18 个子命令（init run debug loop test config mcp plugin cluster marketplace autonomy skill a2a realtime create-edge-agent doctor completion version）——注意是 `ap marketplace`，README:98 的 `ap market` 与实际不符
- **可观测**：Prometheus / OTel / pprof / SLO / Grafana 看板（deploy/grafana 实测 7 个 JSON）
- **生态**：MCP Client+Server、A2A gRPC、K8s Operator（AgentDeployment CRD + HPA）、技能/插件市场（ECDSA 验签）、VS Code + 浏览器插件、Edge 网关（Cloudflare Workers / Vercel Edge，WASI）

### 数字白名单（均有出处，可上屏）

| 数字 | 出处 |
|---|---|
| 20000 并发读 / 7500 并发写 · 0 错误 | README:44、CHANGELOG §5.0.0 |
| 30 分钟稳态 88,182 请求 · 0 错误 | 同上 |
| 9 端点 P95 ≤ 104µs | 同上 |
| SQLite WAL 写入 P95 ↓98%（6.9ms→108µs） | README:44、V5-ROADMAP v4.9 |
| Soak 恢复率 1.0 · 集群 kill-1 退化 0 | README:42-43 |
| Agent 单轮 10.8µs · 工具调用 11.0µs · 记忆检索 46.3µs | agentprimordia/docs/benchmarks/官方基准.md:23-27 |
| 自举成功率 0.33 → 1.00（AP 用 AP 开发 AP） | ROADMAP v3.6、CHANGELOG v6.0 |
| 评估集 160 条 · 跨语言 19 套件 · TS vitest 2692 全绿 | CHANGELOG v6.0、RETEST-v6.0 |

### 禁用黑名单（文档不一致 / 基准过时，宣传勿用）

- 性能对比 23x/7x/8.7x/18x（index.md:135-142，源自 v0.7 + Go 1.22 旧基准）
- QPS 209,107 / 171,703（MockLLM 微基准，官方自注生产延迟由 LLM 往返主导，不能当端到端吞吐）
- 「34 模块 / 100% 对等 / 24+ 模块」三口径打架 → 只说「双 SDK 功能对等」
- 「PII 比正则快 10x」（无出处）；`ap market`（实为 `ap marketplace`）；「20+ 钩子」（实际 35，直接说 35 更有力）；旧版「全部完成」话术（项目状态.md 已标仅作追溯）

## 二、创意概念 v2《万物之源，智能之始 / From Primordia, Intelligence》

**视觉主线**：原初之光 → 有序智能。散乱粒子（混沌问题）汇聚成光球（品牌），生长为集群（编排），最终坍缩回 Logo（收束）。
**点睛呼应**：Studio 概览页主视觉就叫**「原初体」**（哈希环）——片头 AI 概念动画的光球，在 Shot 9 由真实 Studio UI 揭示同构，形成「概念 ↔ 实证」闭环。
**风格**：深空蓝黑底 + 电光青主色 + 紫色点缀 + 发光粒子 + 线框几何体；B 轨录屏统一加深色 UI 边框 + 青色描边，保证两轨色调一致。
**实操**：AI 视频里的文字（Logo / Slogan / 数字卡）全部后期合成，AI 提示词中的文字仅作占位构图。

## 三、主提示词（整片风格总控，每段分镜都带上）

### English（推荐直接投喂）
```
Cinematic tech brand film for "AgentPrimordia", a production-grade AI Agent development framework.
Visual motif: a primordial orb of light evolving into an ordered constellation of agents.
Style: dark futuristic tech aesthetic, deep space navy-black background, electric cyan and violet
accent glows, luminous particle streams, holographic wireframe geometry, volumetric light, shallow
depth of field, ultra-detailed 3D motion graphics, smooth camera dolly and orbital moves, 4K, 24fps,
anamorphic feel, high contrast, premium developer-tool branding (in the style of Vercel / Stripe
product films). No stray text except rendered UI mockups. Consistent color grading across shots.
```

### 中文对照
```
为生产级 AI Agent 开发框架 AgentPrimordia 制作的电影级科技品牌片。视觉母题：一颗原初光球
进化为有序的智能体星座。风格：暗黑未来科技感，深空蓝黑背景，电光青与紫色辉光，发光粒子流，
全息线框几何体，体积光，浅景深，超精细 3D 动态图形，平滑推轨与环绕运镜，4K 24fps，
变形宽银幕质感，高对比度，高端开发者工具品牌片质感（参考 Vercel / Stripe）。
除 UI 模拟画面外不出现杂乱文字，全片色调统一。
```

## 四、分镜提示词 v2（10 幕 × 75 秒：A 轨 AI 提示词 / B 轨真实素材 / 旁白）

### Shot 1 · 混沌开场（0–6s）
- **A 轨**：EN: Extreme close-up of scattered glowing code fragments and broken circuit lines floating in dark void, chaotic flickering particles, faint red warning glow pulses, camera slowly pulls back, tense atmosphere, cinematic macro shot.
- **旁白**：构建生产级 AI Agent，为什么这么难？

### Shot 2 · 品牌揭示（6–14s）
- **A 轨**：EN: The scattered particles converge into a single brilliant cyan orb at the center of darkness; elegant luminous lines ripple outward forming a minimalist wireframe network; two lines of glowing typography materialize, light rays sweep, epic reveal moment.（中文「万物之源，智能之始」+ 英文 AgentPrimordia 由后期合成）
- **旁白**：AgentPrimordia——万物之源，智能之始。一个框架，补齐从原初到智能的全部链路。

### Shot 3 · ReAct 引擎（14–21s）
- **A 轨**：EN: A glowing cyan ring orbits a central neural core, studded with 35 tiny light ticks; a violet "thinking" pulse and a cyan "acting" spark chase each other around the loop; at each pass a soft checkpoint flare lingers, energy trails, macro depth of field.
- **B 轨**：终端 `ap loop trace` 执行轨迹逐行滚动；叠字幕「35 Hooks · 每轮 Checkpoint」
- **旁白**：ReAct 循环驱动思考与行动。35 个生命周期钩子，每轮自动检查点——断点续跑，每一步尽在掌控。

### Shot 4 · 多 Agent 编排（21–28s）
- **A 轨**：EN: Dozens of small glowing agent nodes self-organize into flowing formations — a pipeline line, a branching DAG tree, a circular group-chat ring, then a swarming cloud; thin light beams connect them, messages travel as light pulses along the links, synchronized swarm choreography, aerial camera orbit.
- **B 轨**：`go run ./ecosystem/examples/multi-agent/` 输出 10 任务 × 5 Worker 统计表；叠字幕「Pipeline · DAG · GroupChat · Swarm · MapReduce」
- **旁白**：流水线、DAG、群聊、辩论、Swarm——多模式编排，一个引擎调度，并发原生。

### Shot 5 · 三层记忆（28–35s）
- **A 轨**：EN: Inside a translucent glass brain-like structure, three luminous layers stack — flowing data rivers below, a glowing 3D vector galaxy of star-points clustering by meaning in the middle, cascading document shards above; one beam of light queries down through all layers and returns with a glowing answer, elegant vertical crane.
- **B 轨**：`npx tsx agentprimordia/demo/ts-basic.ts` 记忆/向量分段输出；叠字幕「FTS5 + HNSW + RAG · RRF 融合」
- **旁白**：关键词、向量、RAG——三层记忆，混合检索。它记得住，更找得准。

### Shot 6 · 万模归一（35–42s）
- **A 轨**：EN: A central glowing hub ringed by thirteen light ports; thirteen differently-hued energy streams flow from the rim into the hub and emerge unified as one bright beam; when one stream flickers a secondary path detours smoothly, abstract planet-like orbs orbit gently, smooth orbital camera.
- **B 轨**：ResilientProvider 代码段滚动（primary → fallback → local，README:344-354）；叠字幕「13 Providers · 重试 / 降级 / 熔断」
- **旁白**：十三家大模型，一套 API 统一接入；重试、降级、熔断——主模型抖动，备用无缝接管。

### Shot 7 · 安全护栏（42–49s）
- **A 轨**：EN: A holographic hexagonal shield materializes around a glowing agent core, deflecting red hostile particles tagged as PII; one red particle shaped like a shell command dissolves against the shield into harmless sparks, premium dark UI aesthetic, subtle micro-animations.
- **B 轨**：`npx tsx agentprimordia/demo/ts-basic.ts` 中 ACL/Sandbox 拦截 `rm` 的输出特写；叠字幕「ACL · Sandbox · Guardrails · PII」
- **旁白**：ACL、沙箱、护栏、PII 检测——危险命令，在沙箱里被当场拦截。

### Shot 8 · 生产验证（49–57s）
- **A 轨**：EN: A futuristic data center corridor at night, server racks glowing with cyan LED waves; a shockwave of red chaos lightning strikes — lights flicker, then instantly re-route and stabilize into steady green flow; overhead crane shot rises to reveal an ordered glowing Kubernetes container grid.
- **B 轨**：Studio `/chaos` ChaosLab：创建实验 → 注入 → 报告，三步假设验证逐项打勾；后期数字卡：20000 并发读 · 0 错误 / 30 分钟 88,182 请求 / WAL P95 ↓98% / 恢复率 1.0
- **旁白**：两万并发零错误，三十分钟稳态八万八千请求，写入延迟下降百分之九十八，混沌注入恢复率 1.0——不是宣称，是被证明的生产级。

### Shot 9 · 开发者体验（57–66s）
- **A 轨**：EN: Over-the-shoulder shot of a developer in a dim studio; on a dark IDE screen a few lines of glowing code type themselves, then bloom into the wireframe constellation leaping out as a hologram above the keyboard; split-screen glimpse: the same elegant pattern in Go on the left and TypeScript on the right, converging into one identical agent hologram.
- **B 轨**：终端 `ap init demo` → `ap run` 三步；`./scripts/dev-studio.sh` 一键拉起 → 浏览器打开 Studio Overview——**「原初体」哈希环主视觉与片头光球同构**，此处 1 秒同构叠化；叠字幕「18 CLI 命令 · Go/TS 双 SDK 对等」
- **旁白**：三步创建 Agent，十八条命令覆盖全生命周期；Go 与 TypeScript 双 SDK 功能对等——还有 Studio，一眼看见整个智能体系统。

### Shot 10 · 自治收束（66–75s）
- **A 轨**：EN: All light streams fly back and collapse into the cyan orb, condensing into the glowing "AgentPrimordia" logo mark on pure black; behind it a rising curve of light draws an upward trend from a faint flicker to a steady blaze; the tagline fades in with a soft light sweep, final subtle lens flare, elegant fade to black.
- **B 轨**：Studio `/learning` LearningMonitor 能力进化趋势线上扬；数字卡「自举成功率 0.33 → 1.00」；定版：Logo + 万物之源，智能之始 / From Primordia, Intelligence + 从一行代码，到生产级智能体。
- **旁白**：它甚至用自己的框架开发自己——自举成功率从 0.33 走到 1.00。AgentPrimordia：万物之源，智能之始。从一行代码，到生产级智能体。

## 五、完整旁白稿（75 秒）

**中文**：见第四节各幕旁白，按序连读即可。

**English（精简版）**
> Building production-grade AI agents is hard. AgentPrimordia changes that.
> One framework: a ReAct engine with thirty-five lifecycle hooks, checkpointing every turn.
> Pipeline, DAG, group chat, swarm — orchestrated natively. Three-layer memory that remembers and retrieves.
> Thirteen model providers, one API. Retries, fallbacks, circuit breakers built in.
> ACL, sandbox, guardrails — dangerous commands stopped before they run.
> Twenty thousand concurrent requests, zero errors. Chaos-tested. Recovery 1.0. Production-proven.
> Three steps to your first agent. Go and TypeScript, fully in parity.
> It even develops itself — bootstrap success from 0.33 to 1.00.
> AgentPrimordia. From Primordia, intelligence. From one line of code, to production-grade agents.

## 六、音乐与节奏

- **配乐**：95–110 BPM 电子乐，深 bass 脉冲 + 递进合成器层次；Shot 2 第一个 drop，Shot 8 第二次抬升，Shot 9–10 收束。
- **音效**：粒子 shimmer、护盾冲击、ChaosLab 打勾声、UI 打字/点击微声。
- **剪辑**：Shot 1–2 慢（悬念）→ 3–8 快切（能力轰炸，每幕 6–8 秒）→ 9 中速（真实 UI 主秀）→ 10 收束。
- **两轨衔接**：每幕 A→B 硬切或叠化；B 轨录屏统一加暗色 UI 边框 + 青色描边 + 轻微视差。

## 七、B 轨录屏拍摄清单（按序拍完即剪）

1. `ap init demo && cd demo && ap run` —— 三步创建 Agent（终录屏）
2. `go run ./ecosystem/examples/github-issue-triage/` —— Issue 分类报告表格
3. `go run ./ecosystem/examples/multi-agent/` —— Pool 10×5 并发统计
4. `npx tsx agentprimordia/demo/ts-basic.ts` —— 钩子/记忆/向量/沙箱分段横幅
5. `ap loop trace`、`ap doctor` —— 执行追踪与健康检查
6. `./scripts/dev-studio.sh` —— 一键拉起，浏览器自动打开 Studio Overview「原初体」主视觉
7. Studio：/chaos 三步验证打勾、/cluster 拓扑、/learning 趋势线、/marketplace 部署
8. IDE 分屏：Go hello-agent（README:224-258）vs TS ts-basic.ts

> 拍摄提示：Studio 未接真实服务时响应头标 `X-Data-Source: demo`（屏幕演示无碍）；录屏 ≥2560 宽、光标高亮、等宽字体调大。

## 八、备选风格变体（替换主提示词 Style 段即可）

1. **极简白盒风**：`pure white studio background, soft pastel cyan gradients, clean isometric 3D shapes, matte materials, minimal Apple-style motion design`
2. **赛博工业风**：`rain-soaked neon city rooftop at night, holographic terminals, glitch transitions, teal-and-magenta grade, blade-runner atmosphere`
3. **手绘白板风**：`clean whiteboard sketch style, hand-drawn arrows and boxes animating themselves, marker texture`
