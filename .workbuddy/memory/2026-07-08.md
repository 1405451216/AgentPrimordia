# 2026-07-08 工作日志

## 风险评估与修复

### v1.0.0 深度评估更新
- 对 AgentPrimordia v1.0.0 做二次深度评估（对比 2026-07-07 基线）
- 700 Go 源 / 332 测试，build/vet 全绿
- 全部核心包测试通过（agent/llm/memory/tools/pool/orchestration/security/guardrail/pkg）
- 产出 `PROJECT_EVALUATION.md`，综合评分维持 8.0/10
- 识别 7 个风险项（P1×2 / P2×3 / P3×2）

### 风险修复（已完成）

#### 1. `tools → security` 越界引用（P1）
- 根因：`internal/tools/builtin/shell.go` 调用 `security.ContainsShellMetacharacter`
- 修复：将函数下沉到 `internal/tools/shellcheck.go`，`security` 层改为委托调用
- 新增 `internal/tools/shellcheck_test.go`（覆盖 15 个 case）
- 修改 `sandbox_fuzz_test.go` 移除对 `dangerousChars` 的直接引用（内联列表）
- **验证**：`tools → security` 反向 import 降为 0；`security → tools` 正向依赖建立

#### 2. A2A 异步任务吞错（P2）
- 根因：`internal/agent/a2a/service.go:85` 的 `go func() { _ = ... }()` 静默忽略 handler 错误
- 修复：goroutine 内改用 `s.logger.Error(...)` 记录结构化错误
- 新增 `TestA2AService_CreateTask_AsyncHandlerError` 测试用例
- **验证**：handler 返回 error 时 CreateTask 正常返回，日志路径被触发

### 其他风险处理说明
- **Error code 未贯穿全链路**：架构建议，`errorCodeMapping` 已工作，全仓重构不在此时
- **Provider template 19 TODO**：by design 的 TEMPLATE ONLY 文件，已醒目标注
- **pgvector 适配器 / CI -race / coverage**：功能/CI 配置项，非代码 bug

### 关键技术决策
- `ContainsShellMetacharacter` 下放到 `internal/tools`，`security` 层保留委托
- 此方案优于反向解耦（在 security 定义 interface 让 tools 实现）：保持 `dangerousChars` 单一数据源，避免循环依赖